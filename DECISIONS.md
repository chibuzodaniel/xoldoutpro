# DECISIONS.md

Every assumption made building XOLDOUT, what was chosen, and why. One entry per decision. Source of truth for scope is `xoldout-prd.md`.

## Blocking decisions (answered 2026-08-14, via AskUserQuestion)

- **Platform commission**: 15%, withdrawal fee absorbed by the platform. Shapes `WalletLedgerEntry.kind = COMMISSION_FEE` and `Payout.feeKobo` (currently always 0).
- **Settlement / refund window**: 7-day pending → available, 7-day refund window. One window serves both (PRD explicitly allows this).
- **Payment processor**: Flutterwave — card, bank transfer, and USSD. Chosen over Paystack per explicit user preference; no functional reason to prefer one over the other for this build.
- **Sold-out semantics**: cap is per release; a sold-out release stays permanently sold out; a second pressing must be a brand-new listing (new `Product` row), never a reopened cap on the old one.
- **Auth**: Firebase Auth — email/password and Google sign-in. Session verified server-side via Firebase Admin `verifyIdToken`; the Postgres `User` row is mirrored on first sign-in via `firebaseUid`, created by `POST /api/auth/sync`.
- **Stack/hosting**: Next.js (App Router) + Postgres (Prisma) + Cloudflare R2, Vercel/Railway-class hosting.

## Client-side crop before upload, and transparent logo assets (2026-08-15)

- **Every single-image upload (avatar, cover, release/beat/event artwork, merch product photo) now goes through `ImageCropModal`** (`components/upload/ImageCropModal.tsx`, using `react-easy-crop` — new dependency) before the file ever reaches `uploadImage`/R2. Previously the server (`lib/images.ts`'s `resizeSquare`/`resizeBanner`, `fit: "cover"`) silently center-cropped whatever was uploaded with no user control. The modal's output dimensions match the server's target aspect exactly (512×512 avatar, 1200×400 cover, 1024×1024 square artwork/product photo), so the server's resize is a no-op on framing — the crop the user chooses is the crop that ships.
- **Merch gallery photos (multi-file) are NOT cropped** — deliberate scope simplification. A sequential per-file crop flow for a multi-select input is meaningfully more UX complexity than the primary single-image case; revisit if it becomes a real pain point.
- **Logo backgrounds removed**: `xoldout-logo.jpeg` (wordmark, used on login/signup) was flattened on solid black with no alpha channel. Recovered transparency via an unpremultiply-against-black pass (`out = observed * 255 / max(r,g,b)`, alpha = `max(r,g,b)`) rather than a hard chroma-key threshold, since it handles anti-aliased edges cleanly — same technique implied by the already-existing `xoldout-icon-transparent.png`. Produced `xoldout-logo-transparent.png`; login/signup now use it.
- **`apple-touch-icon` (`xoldout-icon.jpeg`) was deliberately left opaque**, not switched to a transparent PNG — iOS fills transparent regions of home-screen touch icons with black rather than compositing against wallpaper, so a transparent version wouldn't actually look "background-removed" there, just less predictable. Every icon reference that actually renders in-page already used the pre-existing `xoldout-icon-transparent.png`.
- **Bug found after shipping this**: the crop modal appeared broken on `profile/edit` (avatar/cover) right after this landed. Root cause was environmental, not a code bug — `react-easy-crop` was `npm install`ed while `next dev` was already running, and Turbopack's dev server held a stale module graph from before the package existed in `node_modules`. Verified the component itself was correct (both `cropShape="round"`/aspect 1 and `cropShape="rect"`/aspect 3 render, pan, zoom, and crop correctly) via a throwaway unauthenticated debug route before concluding this. **Takeaway: always stop and restart `next dev` after installing a new dependency, not just after schema changes** — a `.next` cache clear plus restart resolved it.

## Own-profile awareness on the public profile page (2026-08-15)

- **`/u/[handle]` previously looked identical whether you were viewing your own public profile or someone else's** — no way to tell "this is you" from "just viewing." `ProfileHeaderRow` (`components/profile/ProfileHeaderRow.tsx`) fixes this: a client component that checks `appUser.id === targetUserId` and swaps the avatar-with-edit-badge + "Edit profile" link (mirroring the exact pattern already on the private `/profile` page) in for the plain avatar + `FollowButton` when you're looking at yourself.
- This has to be a client-side check, not server-side: `/u/[handle]` is a Server Component and this app's auth is bearer-token-based (Firebase ID token attached per-fetch via `apiFetch`, no session cookie), so the server has no way to know who's viewing during SSR. `FollowButton` already worked around this the same way (`useAuth()` + returns `null` when `appUser.id === targetUserId`).

## Migration history is out of sync with the schema (2026-08-14)

- `npx prisma dev`'s shadow database (used to diff migrations before writing a new one) is persistently broken — every `migrate dev` attempt after the initial one fails with `type "LedgerKind" already exists`, even immediately after a clean `db push`. Unlike the connection-pool crashes elsewhere in this file, this one isn't intermittent — it reproduces every time.
- **The `User.socialLinks`/`User.pushEnabled` columns (added for the Settings redesign) were applied via `npx prisma db push`, not `migrate dev`** — so there is no corresponding file in `prisma/migrations/`. `prisma/schema.prisma` is the accurate source of truth for the current schema; the migrations folder is one change behind it.
- **The Beat/Event/TicketTier/TicketCheckIn/MerchItem/MerchOrderFulfillment tables (2026-08-15) were also applied via `db push`**, for the same reason — the drift above compounds by one more schema change now.
- **Before deploying anywhere with `prisma migrate deploy`** (which only replays files in `prisma/migrations/`, never the schema directly), someone needs to either: (a) run `prisma migrate dev` once against a real, non-`prisma-dev` Postgres to generate a baseline migration matching current `schema.prisma`, or (b) hand-write the missing migration SQL (`ALTER TABLE "User" ADD COLUMN "socialLinks" JSONB NOT NULL DEFAULT '[]', ADD COLUMN "pushEnabled" BOOLEAN NOT NULL DEFAULT false;`) and mark it applied with `prisma migrate resolve --applied`. Do this before the next `migrate dev` call, not after — otherwise the drift compounds.

## Naming override (2026-08-14)

- **The MVP tab labeled "Fanbase" in the PRD is called "Socials" in the app** (`/socials`, was `/fanbase`), per explicit user instruction. Note this collides with the PRD's own Phase 3 vocabulary, where "Socials" names a *different*, not-yet-built feature (a public algorithmic feed, distinct from the private following-based announcements this tab actually shows — PRD §5/§11). If/when Phase 3 Socials gets built, the naming will need to be reconciled — either this tab gets renamed again or the P3 feature gets a different name.

## Mid-build change (2026-08-14)

- **Firebase scope narrowed to Auth + push (FCM) only.** Originally proposed Auth.js with Credentials+Google; user redirected mid-build to Firebase Auth specifically, plus Firebase Cloud Messaging for push notifications. Postgres/Prisma remained the system of record for everything else (money, stock, entitlements) — nothing in the approved architecture/schema plan changed except the auth provider. `User.fcmTokens` added to receive push later; no push-sending code exists yet (nothing in Milestone 1/2 requires it — revisit when Fanbase announcements or purchase confirmations are built).

## Should-know defaults (PRD §20 — no objection raised, proceeding with recommended default)

- Money stored as integer **kobo**, never floats (`priceKobo`, `amountKobo`, etc. throughout).
- Free releases **may** carry a quantity cap (`priceKobo = 0` and `StockPolicy.cap` are independent).
- Default preview length when a track is ≥30s: **30s**. Shorter tracks default to a custom length capped at their own duration.
- Pricing is **per release only** in MVP — no per-track pricing within an EP/album.
- Upload limits: audio ≤ **200MB** per file, ≤ **30 tracks** per release.
- Repo root is `xoldOutpro/` (this directory); PRD files stay here, app code lives in `web/`.
- No Figma/brand file was provided — UI built from the design tokens and screen reference in `xoldout-prd.html` (dark theme, serif display type, `#E11D2E` accent).
- No staging environment yet — local (`prisma dev`) + production only.

## Beat/Event/Merch built for real, overriding the PRD's MVP boundary (2026-08-15)

- **The PRD explicitly says "do not build or scaffold" Beats/Events/Merchandise** (Phase 2/3). New reference screenshots showed a fuller Discover page with these sections populated, and the user chose to build them for real now rather than fake the UI or defer — an explicit, knowing override of the PRD's scope boundary, not a misreading of it.
- **Beat licensing**: single flat license only (buyout-style — one price, full commercial-use rights forever). No lease/exclusive tiers. Reuses the exact same audio ingest pipeline as music tracks (`POST /api/uploads/audio/ingest`): `audioStreamUrl` is the pre-purchase preview rendition, `audioMasterUrl` is what `presignDownload` serves after purchase, mirroring `app/api/tracks/[id]/audio-url`.
- **Merch fulfilment**: creator-shipped. Platform only collects payment and a shipping address (`MerchOrderFulfillment`, one per Order); the creator ships and self-reports `TO_SHIP → SHIPPED → DELIVERED`. No platform warehouse/inventory system, no courier integration.
- **Merch has no size/color variants in v1** — one `Product` = one listing = one price = one `StockPolicy`, same shape as a Release. Matches the reference screenshots (no variant picker shown) and keeps the commerce core untouched. Revisit if a creator actually needs per-variant stock.
- **Event ticket tiers are `Product` rows, not a field on `Event`.** A new `Event` model is a parent listing (venue, date, cover image); each tier (Early Bird/Regular/VIP) is its own `Product` (`type: EVENT`) with its own `StockPolicy`/price, linked back via `TicketTier.eventId`. This is a deliberate deviation from the PRD's diagram, which shows `Event` as a direct `Product` subtype — that shape can't support independently-priced/capped tiers without inventing a second stock/entitlement mechanism. Buying "VIP" is buying that tier's `Product`, so `reserveStock`/`confirmStock`/`recordSale`/`Entitlement` all apply unchanged. `Event.status`/`publishedAt`/`deletedAt` cascade to its tier Products on publish/delete.
- **Event check-in**: `TicketCheckIn` is a 1:1 extension of `Entitlement` (only created for `EVENT`-type purchases), holding a unique `code` rendered as a QR in the buyer's Library. Scanning uses the browser `BarcodeDetector` API where available, with a manual code-entry fallback — no new scanning dependency. QR *generation* uses the new `qrcode` npm package.
- Full architecture/schema plan (including the reasoning above) is preserved at `C:\Users\user\.claude\plans\reflective-fluttering-volcano.md`.

## Closing the Beat/Event/Merch scope gaps (2026-08-15)

All four gaps from the previous section are closed:

- **Merch shipping fee**: `MerchItem.shippingFeeKobo` (creator-set, defaults to 0) is added to `Product.priceKobo` at checkout (`amountKobo` in `app/api/orders/route.ts`); a free item with a nonzero shipping fee now correctly routes through the paid Flutterwave path instead of the ₦0 free-grant path. The fee actually charged is snapshotted onto `MerchOrderFulfillment.shippingFeeKobo` so a later fee edit on the listing never changes what a past buyer paid — same snapshot pattern as `OrderItem.priceKobo`. Platform commission applies to the fee too (folded into `payment.amountKobo`, same as the item price) — simplest consistent choice, no special-casing in `lib/commerce/ledger.ts`.
- **Search and public creator profiles** (`/api/search`, `/u/[handle]`) now include Beat and Merch products, not just Release — `ProductCard` was already type-generalized and needed no changes. Event was deliberately left out (ticket tiers need `EventCard`'s different shape, not a Product grid).
- **Per-tier event editing**: `PATCH /api/events/[id]/tiers/[tierId]` lets a creator edit a tier's price (freely, no restriction) and cap (lowering-only, same rule as Release — never raised, never below tickets already sold), with inline controls in `profile/catalog/events`.
- **Beat playback now uses the shared `PlayerProvider`/mini-player**, not its own `<audio>` element — `PlayableTrack` gained an optional `kind?: "track" | "beat"` (defaults to `"track"`, so every existing Release call site is unchanged) that switches which audio-url route gets hit and skips the offline-cache lookup (beats have no offline format). Confirmed by tracing the code that Release playback is byte-for-byte unaffected.

## Implementation notes / scope calls made while building Milestone 1

- **Local Postgres**: `npx prisma dev` (Prisma's bundled local Postgres, no Docker) rather than a hosted instance, since this is local development. Swap `DATABASE_URL` for a real Postgres connection string before deploying.
- **Waveform peaks + streaming transcode run synchronously** inside `POST /api/uploads/audio/ingest` (ffmpeg via `ffmpeg-static`) rather than through the `Job`/worker-queue design in the architecture plan. The `Job` table still exists in the schema for the future move to async processing under load (large catalogs, slow uploads) — for Milestone 1's scale, synchronous is simpler to develop and test end-to-end without standing up a second always-on process. `STOCK_SWEEP`/`SETTLEMENT_SWEEP` jobs (releasing expired stock holds, flipping pending ledger entries to available) are Milestone 2 work and still need the queue — that's when the worker actually needs to exist.
- **Streaming audio rendition is MP3** (`libmp3lame`, 128kbps), not AAC or Opus/WebM — chosen because MP3 needs no seek table/container (streams fine from a plain byte pipe) and plays on every browser including iOS Safari, which WebM/Opus does not reliably support.
- **Avatar/cover are single server-resized derivatives**, not a multi-size ladder — avatar 512×512, cover 1200×400. Release artwork *does* get a real ladder (64/256/1024) since it's shown at genuinely different sizes across Home/catalog/detail surfaces and the PRD calls this out explicitly (§7.1); profile images are shown at one size in practice, so a ladder there would be unused complexity.
- **Release cap editing**: only lowering an already-set cap is supported (down to units sold, matching the PRD's 500→100-not-50 example). Introducing a cap on a previously-uncapped release, or removing a cap entirely, is rejected — both would let scarcity be gamed after the fact, which the PRD doesn't sanction and the "cannot raise" rule implies.
- **`npx prisma dev`'s embedded Postgres is fragile under concurrent load, even with the tuned pool params below.** It repeatedly crashed outright (not just throttled) during this build whenever several separate Node processes hit it around the same time — the stock-race test's own concurrent connections, `next build`'s static-generation workers, and a lingering `next dev` instance were each enough on their own to kill it. `npx prisma dev ls` kept reporting "running" after each crash; the only reliable fix was `npx prisma dev stop <name>` then `start <name> -d` and re-verifying with `prisma db execute` before doing anything else. If this keeps happening on your machine, swap in a real local Postgres (Docker, or a free-tier hosted instance) — the embedded dev server is convenient but not built for this kind of concurrency testing.
- **Local Postgres connection string needs explicit pool params.** `npx prisma dev`'s embedded Postgres recommends (and effectively requires) `connection_limit=10&connect_timeout=0&max_idle_connection_lifetime=0&pool_timeout=0&socket_timeout=0` on `DATABASE_URL` — without them, a burst of ~10+ concurrent connections (exactly what the stock-race test does) killed the local dev server outright rather than queuing. `lib/db.ts` also caps the `pg.Pool` at `max: 10` to match. This is a local-dev-only concern; a hosted Postgres in production can use its own pool sizing.
- **Release detail page (`/r/[id]`) ships in Milestone 1 as browse-only** — artwork, price, remaining/sold count, tracklist, no player and no buy button yet. Buying, entitlements, and playback are explicitly Milestone 2 (PRD §19); building a real player against non-existent entitlements would need to be redone anyway.

## Fanbase (Socials) announcements built — Milestone 3 item 11 (2026-08-17)

- **`Post`/`PostLike` are new top-level models**, not nested under a `FanbaseGroup` — groups are Phase 2 and don't exist yet. Any account may author a post (PRD §1.4: identity never gates capability); it reaches whoever follows that account. `GET /api/posts` returns posts from `{followed creator ids} ∪ {self}` — own posts are included so the composer has visible effect without following yourself, a minor addition the PRD doesn't specify either way.
- **No pagination** — `GET /api/posts` returns the latest 50 posts, matching the simplicity level of `/api/search`'s `take: 20`. Revisit with a cursor if a real feed ever exceeds that.
- **Post images reuse the existing `artwork` upload kind and square-crop pipeline** (`/api/uploads/artwork/finalize` → `artworkLadder`, picking the `1024` size), same as merch gallery photos — no new resize function, no crop UI (consistent with the "Merch gallery photos... NOT cropped" precedent above). The PRD's own Fanbase mockup shows a wider rectangular image, but reusing the square pipeline avoided a second image-processing path for one screen.
- **Post body capped at 500 characters** — PRD doesn't specify a length for posts (only the 2,000-char cap on release/track descriptions); 500 fits an announcement without inviting essay-length posts.
- **This screen (`/socials` in the app, "Fanbase" in the PRD — see the naming override above) had to be built as a client component fetching `/api/posts` via `apiFetch`**, not a Server Component reading `db` directly like `/discover` — this app's auth is bearer-token-only with no session cookie (see "Own-profile awareness" above), so the server can't identify the viewer during SSR. Same pattern as `/library`.

## Trust & safety built — Milestone 3 item 13 (2026-08-17)

- **`Report` (already scaffolded in the schema, unused until now) gained `eventId`/`postId`/`profileId` FKs and a `details` free-text field**, alongside the existing `productId`. `ReportTargetType` gained `EVENT` — Event is deliberately not a `Product` (DECISIONS.md, Beat/Event/Merch section: ticket tiers are the Products), so it needs its own target column; a report on an event page can't be filed against a tier without being confusing to a moderator reading the queue. `COMMENT` stays FK-less since Comment doesn't exist until Phase 2 groups.
- **`User.isModerator`** is a plain boolean, set directly in the DB — there's no self-serve UI or promotion flow, matching PRD §3's "Platform moderator: Internal." `requireModerator()` (`lib/auth/session.ts`) 403s non-staff; `AuthError` gained a settable `status` (was hardcoded 401) to support this.
- **In-context reporting is scoped to the two content-moderation reasons** (Inappropriate content, Copyright claim) on `PRODUCT`/`EVENT`/`POST`/`PROFILE` — `ReportButton` hides for the owner/author and for signed-out visitors. Bug/Feature request have no natural "target" (the PRD's report table doesn't map them to content), so they're filed through a separate "Send feedback" entry point on the private Profile page, self-targeted as `PROFILE`/own `userId` by convention — the API's "can't report your own profile" guard is relaxed specifically for those two reasons so this doesn't 400.
- **Automatic device/app-version/screenshot attachment on bug reports (PRD §14) was not built.** Screenshot capture specifically would need either a new client-side capture dependency or a permissions-gated browser API; out of scope for this pass. `details` is free text only. Flag if bug-report volume ever makes this worth it.
- **Copyright takedown (`PATCH /api/reports/[id]`, `action: "takedown"`) is the one report action that moves money**, and is intentionally the *only* path in the codebase that revokes an `Entitlement` outside of the reasoning in PRD §8 ("never revoked by artist action, only by refund") — it qualifies because it *is* a refund: `lib/commerce/ledger.ts` gained `recordRefund()`, a single `REFUND_DEBIT` ledger entry for the net amount (gross − commission) the seller was credited, effective immediately (`availableAt: null`) rather than after the usual 7-day window — a seller holding funds from a voided sale owes them back now. The transaction also sets `Product.status = DELETED` (same mechanism as a creator's own delete) and `Order.status = REFUNDED` for every affected order. Restricted to `reason: COPYRIGHT_CLAIM` + `targetType: PRODUCT` only — Event ticket-tier takedowns would need to reverse N tier-Product sales at once and aren't handled by this action yet.
- **The Flutterwave side of a refund — actually returning the buyer's money via `/transactions/{id}/refund` — was deliberately NOT wired in.** Everything the ledger needs (the debit, the revoked entitlement, the `REFUND_DEBIT`-triggered negative-available-balance-until-settlement) is real and auditable in Postgres, but calling a live payment processor to reverse a real charge is an external, hard-to-reverse financial action that shouldn't be added speculatively. Flag this explicitly before the takedown flow is used against a real (non-test) Flutterwave transaction — the buyer's card/bank won't actually be refunded yet, only the platform's internal ledger.
- **Verified the full takedown transaction (product→DELETED, entitlement revoked, order→REFUNDED, correct `REFUND_DEBIT` math) end-to-end** by seeding a simulated paid order directly via Prisma (bypassing Flutterwave entirely — no live payment call was made) and running the actual takedown through the moderation UI. Confirmed the ledger entry exactly zeroes out the original `SALE_CREDIT`/`COMMISSION_FEE` pair.

## Analytics built — Milestone 3 item 12 (2026-08-17)

- **`GET /api/analytics` computes everything from existing tables — no new schema.** Units sold and per-product sell-through/time-to-sell-out come from `Product`/`StockPolicy` (`soldOutAt` minus `publishedAt`, in hours); fan growth is `Follow` rows for/within the last 30 days; returning customers is derived from grouping `PAID` `Order.buyerId` by count (>1 paid order from this creator = returning) — none of this needed a new table, matching the PRD's own framing of `StockPolicy.soldOutAt` as "what makes time-to-sell-out reportable."
- **No Naira figures anywhere on this screen**, per PRD §13 ("Analytics carries units, fans, conversion, and retention, and shows no Naira amounts") — enforced by construction, since the query never selects `priceKobo` or touches `WalletLedgerEntry`.
- **"Top products" and the sell-through/time-to-sell-out figures are one combined list, not two**, sorted by units sold — the PRD lists them as separate bullet points but they're the same underlying per-product data; a second list would just repeat the same five products with different columns.
- **Sell-out rate and returning-fans tiles show "—" (not 0%) when there's no capped product or no customer yet** — 0% would misleadingly claim "nothing sold out" for a creator with no capped releases at all, a distinction the PRD doesn't call out but that avoids a materially wrong statistic on a brand-new profile.
- **No charting/sparkline** — stat tiles plus lists, matching the existing Profile page's Fans/Sales tile pattern exactly. The PRD requires the metrics, not a visualization; the `dataviz` skill exists for later if fan-growth trend lines become worth the complexity.
- **Fan-growth is a single 30-day delta, not a time series** — `newFans30d` alongside the all-time `fans` count. A proper growth chart would need historical snapshots or a `Follow.createdAt` bucketed query per day/week; the PRD's "fan growth" bullet doesn't specify granularity, and a single delta is the smallest thing that's still informative.
- **Verified end-to-end against real data**: reassigned the takedown-test seed product to a real account and added two `Follow` rows, then confirmed in the browser that units sold, fan count, the 30-day delta, and the top-products row all matched the seeded numbers exactly, with sell-out rate and returning-fans correctly showing "—" for the uncapped/single-customer case.

## PWA install guide built — Milestone 3 item 14, closes out the Milestone 3 audit (2026-08-17)

- **Platform detection is event-driven for Chromium, UA-sniffed only for iOS.** `components/pwa/InstallSheet.tsx` listens for `beforeinstallprompt` (Chrome/Edge, desktop or Android) and calls its captured `prompt()` on click — no UA sniffing needed there since the browser tells you directly whether it can install. iOS Safari never fires that event and has no programmatic install API at all, so it's the one browser detected by user agent (`iPhone|iPad|iPod`, plus the iPadOS-reports-as-Mac case caught via `maxTouchPoints > 1`) and shown manual "tap Share → Add to Home Screen" steps instead. Everything else (Firefox, browsers where the event doesn't fire within ~800ms) falls back to a generic "look for Install app in your browser's menu" message rather than showing nothing.
- **Already-installed state** (`display-mode: standalone` or iOS's `navigator.standalone`) shows a "You're set" confirmation instead of install instructions, so a user who installed on a previous visit doesn't see a stale prompt.
- **Revised same-day, on user feedback: this is a bottom sheet, not a full-screen step or route.** The first pass made it a second full-screen onboarding step and a standalone `/install` page; both covered the whole viewport, which read as heavier than the guide warrants. It's now `InstallSheet` — same slide-up/dim-backdrop shell as `PublishSheet`/`ReportSheet` — mounted once globally via `InstallGuideProvider` (`app/layout.tsx`, alongside `PlayerProvider`/`BottomNav`) so it can be triggered from anywhere and floats over whatever page is already showing rather than being its own destination. Onboarding now navigates to `/discover` immediately on profile-setup submit and opens the sheet on top of it (`installGuide.open()` right after `router.push`); Profile → "Install app" opens the same sheet in place instead of navigating to a page. The standalone `/install` route and the `NO_NAV_EXACT` entry for it were removed — nothing needs a dedicated URL anymore.
- **Footer is a small corner × to dismiss, not a full-width "Maybe later" bar** — also revised on feedback. A primary action button (red, full-width) only appears when there's an actual action to take: "Install XOLDOUT" when `beforeinstallprompt` was captured, or "Done" once installed. The iOS-steps and generic-fallback branches have no primary button at all, since there's nothing programmatic to click — just the corner ×.
- **Taste selection was not built** — the PRD's onboarding row lists "PWA install guide, taste selection" together, but taste selection is out of scope for "PWA install guide" specifically, and nothing elsewhere in the app has a personalization/taste signal to select from yet.
- **No service worker was built.** PRD §16 lists "service worker plus encrypted local audio cache" as the offline requirement — the encrypted local audio cache (`lib/offline/*`, IndexedDB-based) already exists and doesn't depend on a service worker, but there's no app-shell service worker for offline page-loads or a true `beforeinstallprompt`-independent install path on browsers that require one. This is a materially bigger feature (cache versioning/invalidation strategy, stale-while-revalidate for API routes, testing across a service-worker lifecycle) than "install guide" scope covers — flag separately if offline-first page loads (not just offline audio playback, which already works) become a requirement.
- **Manifest icon was left as-is** (`public/manifest.json`, one 1080×1080 `purpose: "any"` icon) — didn't add a `maskable` variant, since the existing icon has no safe-zone padding and would risk cropping on Android's circular/rounded mask without new asset work. Out of scope for wiring up the install guide itself.
- **Verified end-to-end after the sheet revision**: signed up a fresh test account, confirmed submitting profile setup lands on `/discover` with the install sheet slid up on top of it (Discover visibly dimmed behind, not covered); closed via the corner ×, landing cleanly back on `/discover`. Separately confirmed Profile → "Install app" opens the same sheet over the Profile page (Wallet/Requests visible dimmed behind it) without navigating away, and the × returns to a normal, fully-interactive Profile. Cross-checked the iOS-detection regex/touch-point logic against five UA cases (iPhone, iPad-as-Mac, real macOS, Android Chrome, Windows Chrome) directly, since this browser can't be made to genuinely report as iOS Safari.
- **Found and fixed a real timing bug mid-review**: the `beforeinstallprompt` listener originally attached only when the sheet opened, not when the app loaded — since Chrome fires that event once, early, and doesn't replay it, a listener attached late would silently miss it forever and the "Install now" button could never appear. Moved the listener into `InstallGuideProvider`'s mount effect (fires once for the app's lifetime) so it's armed before any user could plausibly open the sheet.
- **Could not get Chrome to actually fire `beforeinstallprompt` in this Claude-in-Chrome automation session**, even after the fix above and confirming the manifest itself is valid (200, correct fields, secure context, not already standalone) — this is a known characteristic of extension/remote-debugging-controlled browser sessions, which typically don't satisfy Chrome's install-prompt engagement heuristics, not a defect in the app. Left as-is per user direction ("leave it for now"); should be spot-checked in a normal, non-automated Chrome/Edge window before relying on it. The generic "look for Install app in your browser's menu" fallback and the iOS manual-steps path both work regardless and don't depend on this event at all.
- **User raised a native app (React Native/Capacitor-class wrapper) as a possible future direction**, not committed to — PRD §16 already flags "store distribution is a later decision" explicitly. No code changes made toward this; noting it here so it isn't lost.

## Verification, Collections, Gifting, Fanbase groups — the rest of Phase 2 in one pass (2026-08-17)

User asked to build all four remaining Phase 2/gap items together, plus "make more use of cards where necessary instead of lists." Each is scoped and verified independently below; card-grid treatment (matching `ProductCard`'s tile language) was applied to every new listing surface — Collections, Gifts, Groups — rather than the divided-list-row pattern used for the MVP screens.

**Verification (PRD §12)**
- `User.isVerified` is a plain moderator-toggled boolean, same shape as `isModerator` — PRD §18 leaves the actual criteria ("who gets a badge, on what evidence") explicitly unresolved, so this deliberately doesn't invent a review workflow, just the toggle and the badge. `POST /api/admin/verify` (moderator-gated) + a "Verify a creator" handle-lookup panel on `/moderation`.
- `VerifiedBadge` renders next to the display name on own Profile, public `/u/[handle]`, and post/group-post author names — anywhere a creator name is already shown prominently.

**Collections (PRD §10 Phase 2)**
- `Collection`/`CollectionItem` — a thin grouping over `Entitlement`, not a copy of it; deleting a collection (`onDelete: Cascade` on the item join row) never touches ownership. Library gained a three-tab switcher (Purchased/Collections/Gifts) reading its initial tab from `?tab=`, replacing the single flat view.
- Every Purchased-tab row (release, beat, merch, ticket) gained a small "+" that opens `AddToCollectionSheet` — list existing collections or create one inline, matching the sheet shell used everywhere else.
- Collection items render as a card grid (`EntitlementCard`, shared with the Gifts tab) linking straight to the product's own detail page rather than reimplementing per-type playback/download/QR inline — that logic already lives in Library's row components and shouldn't be duplicated a third time.

**Gifting (PRD §7.3 Phase 2)**
- Reuses the *exact* existing checkout/webhook pipeline — `Order.isGift` is the only new field on the purchase path. `reserveStock`/`confirmStock`/`recordSale` are untouched; the only branch is at entitlement-creation time: a gift order creates a `Gift` row (PENDING, `claimToken`, 7-day `expiresAt`) instead of an `Entitlement`. This is what makes "stock decrements at purchase, not claim" (PRD's explicit requirement) fall out for free rather than needing new logic.
- **Claim mechanics** (PRD §18 open decision, not resolved by the PRD itself): link-based, not handle-targeted — `/gifts/claim/[token]`, publicly viewable, claim requires login (`login?next=...` — added `next` param support to the login page for this, since nothing previously needed a post-login redirect). 7-day expiry chosen to match the existing settlement-window length, not because the PRD specifies a number.
- **Scoped to RELEASE/BEAT/EVENT-tier, not MERCH** — a claim link has no recipient identity until claimed, so there's no shipping address to gift a physical item to.
- **Expiry sweep reuses `recordRefund`** (built for the copyright-takedown path) and a new `releaseConfirmedUnit` stock helper (the inverse of `confirmStock` — undoes the sold-count increment, un-sets `soldOutAt` if back under cap) — both wired into the existing `sweep-holds` cron rather than a new job, extending the same "expired hold → release + refund" shape that already existed for abandoned checkouts.
- Gift purchase is a second button ("Gift this") next to Buy on Release/Beat/Event-tier detail pages, not a separate flow — verified the full loop: create → claim-link preview page → claim → entitlement granted, end-to-end reasoning matches the takedown-refund verification approach from the Trust & Safety pass (seeded/traced through code, since exercising a full 7-day expiry live isn't practical to wait out).

**Fanbase groups (PRD §11 Phase 2 — the biggest piece)**
- `FanbaseGroup`/`Membership`/`JoinRequest` plus extending the existing `Post` model with `groupId` (null = MVP announcement, set = posted inside that group) rather than a parallel `GroupPost` model — reuses `PostLike`, and now `Comment`/`Poll`/`PollVote` for free. The MVP announcement feed (`GET /api/posts`) explicitly filters `groupId: null` so group posts never leak into it — the two stay visually and functionally distinct, matching the PRD's own MVP-vs-Phase-2 split.
- **The creator is always given a `Membership` row with `role: ADMIN` at group creation** — this single decision is what lets every downstream permission check (`postPermission: ADMINS`, admin-list display, join-request approval) treat "creator" as just "the founding admin" instead of needing a separate `creatorId === userId` branch everywhere except settings changes and admin promotion, which PRD §11 reserves for the creator specifically.
- **Default `postPermission: CREATOR_ONLY`** per PRD §11's explicit instruction ("an artist should opt into moderation load deliberately"). Verified live with two accounts: a `CREATOR_ONLY` group correctly blocked a second member's post server-side (`"You can't post in this group"`) even before the client-side composer-hiding fix landed — the permission check was never just a UI affordance.
- **Comments are flat, not threaded** — PRD Phase 2 scope is "member participation," not a nested comment tree. Reachable via a generic `/api/posts/[id]/comments` (works on any Post) but only the group post view renders the UI; the MVP feed stays comment-free per PRD §11 MVP.
- **Polls are a `Post` extension (1:1 `Poll`), not a separate content type** — `options: String[]` + `PollVote(pollId, userId, optionIndex)`, one vote per user (upsert). Proportional result bars only reveal percentages once you've voted (`myVote !== null`), matching common poll-UI convention and avoiding influencing votes with visible tallies beforehand — not a PRD requirement, a judgment call. Verified live with two accounts voting for different options: 50/50 split rendered correctly, each account's own choice highlighted.
- **Found and fixed a real gap during verification, not just a lint issue**: the group composer initially rendered for every member regardless of `postPermission`, relying entirely on the server 403 to stop a disallowed post — functionally safe (confirmed: the server did block it) but a bad UX trap, since a member could type a full post and only discover they couldn't post it after hitting Post. Added a client-side `canPost` check (mirroring the server's exact `CREATOR_ONLY`/`ADMINS`/`ALL_MEMBERS` logic) so the composer simply doesn't render for members who can't use it.
- **`/groups` (list + discover + create) and `/groups/[id]` (detail, feed, join/leave, manage) are new top-level routes**, not folded into `/socials` — Socials gained a small "Groups →" link instead. PRD §5's Phase 3 nav diagram eventually makes Fanbase a groups-first surface, but restructuring the MVP tab bar itself is out of scope here; this keeps groups reachable without disturbing the existing Socials/announcements screen.
- Verified end-to-end with two real accounts: created a group (open visibility), posted with a poll, voted, commented — then joined as a second account, confirmed the post-permission block, confirmed the second vote split the poll 50/50 with correct per-user highlighting, and opened the Manage sheet's all three tabs (Requests/Members/Settings) as the creator. Join-request approval (`REQUEST_TO_JOIN` visibility) and admin promotion were verified by code trace, not live-clicked, given time already spent on the OPEN-visibility path above.

**Cross-cutting fixes found during this pass**
- **`LoadingSpinner`'s `full` variant was measurably miscentered** — its old `min-h-[Xvh]`-based box wasn't stretched to fill its actual parent (most call sites nest it inside plain block divs, not flex containers), so it centered within a too-short, top-anchored box rather than the visible viewport. Replaced with `fixed inset-0 flex items-center justify-center pointer-events-none`, which centers against the true viewport regardless of ancestor layout — fixes every `full` call site at once, not just the ones touched in this pass.
- **The loading icon's animation changed from a flat linear spin to a combined rotate+scale "breathe"** (`animate-spin-breathe`, one breath per full rotation) per explicit request — added as a new keyframe in `globals.css` alongside the existing `waveform-bar` one.

## For You feed ranking (2026-08-23)

- **`GET /api/posts?feed=forYou` is now ranked by affinity, not pure `createdAt desc`** — this does not reopen the earlier "no pool restriction" decision above: the pool (every public post, any creator, except your own) is unchanged, only display order changes. Signals: explicit follow, likes/comments given (90-day window), `TrackPlay` counts (90-day window), active `Entitlement`s (not time-windowed — a past purchase is a lasting relationship), 2-hop follow-network proximity ("creators the people you follow also follow"), and `User.tags` overlap. Combined with a small recency-decay term so a brand-new post from an unrelated creator still surfaces reasonably — For You must stay genuine discovery, never fully collapse to "creators I already know."
- **Scored live per-request, not via a precomputed table refreshed by a cron.** Every signal query is bounded by the *viewer's own* history (their follows/likes/plays/purchases), not a global scan, so this is cheap at current scale — matches the existing convention in `lib/socials/unread.ts` of computing derived per-user social state live rather than caching it. Deliberately avoided adding a new Prisma model for this: this project's prisma schema changes don't automatically reach production (a recurring source of live-page breakage), so skipping a migration here removes that risk entirely. Revisit with a precomputed table only if per-request cost ever actually becomes a problem — same spirit as the "no pagination... revisit with a cursor" note above.
- **The `Job`/`JobType` table in the schema was confirmed unused** (nothing creates or consumes it; "polled by the worker service" describes a worker that doesn't exist in this repo) — not built on for this feature. The only real recurring infra is the three Vercel Hobby crons in `vercel.json` hitting `app/api/internal/*/route.ts` synchronously; this feature needed neither that nor the `Job` table.
- Ranking logic lives in `lib/socials/forYouRanking.ts` as a pure `scoreForYou()` function (fabricated-input unit-testable) separate from `fetchForYouSignals()` (the actual DB queries) — kept apart specifically so the scoring math doesn't need the integration-test-against-real-Postgres style used by `lib/commerce/stock.test.ts`.
- Candidate pool widened to 150 posts internally before ranking+truncating to 50 for the `forYou` branch only, so ranking has something to reorder among — the public "50 posts, no pagination" response contract is unchanged.

## Library "Purchased" tab: grid tiles for Music & Beats (2026-08-23)

- **Releases and beats now render as a 2-column artwork-tile grid** (square cover, title + creator below), replacing the previous flat list of rows — matching an Apple Music Library reference screenshot the user supplied. Tickets and Merch deliberately stayed as list rows: their data (event date/QR/check-in status, shipping status) doesn't fit a plain artwork tile the way an album cover does.
- **Tapping a tile plays immediately and expands inline** (not a navigation to a detail page) — a full-width panel opens directly under the tapped tile's row (`col-span-2` inserted into the grid flow via `flatMap`) showing the release's tracklist with per-track play/download, or a single Download action for a beat, plus "+ Collection". Tapping the same tile again collapses the panel without stopping playback. This was an explicit choice over "tap navigates to /r/[id]" — kept play controls reachable without leaving Library, closer to how the previous list already worked.
- **Removed the "X MB downloaded on this device" usage counter** from the Purchased tab (was `formatBytes(usageBytes)` via `totalDownloadBytes()`) — explicit ask, no replacement. `totalDownloadBytes()` itself stays in `lib/offline/downloads.ts` since nothing else needed it removed and a future storage-management screen might still want it.

## Verification system — backend (schema/migration + lib + API), UI not yet built (2026-08-23)

The blunt `User.isVerified` moderator toggle from the earlier Verification pass (PRD §12, above) stays exactly as-is for backward compatibility. This adds a real application/review workflow (`VerificationApplication`/`VerificationDocument`/`VerificationAuditLog`) on top of it — approving an application sets `isVerified` (never unsets it) and additionally records which specific `VerificationBadgeType` was granted, so the UI can eventually show e.g. "Seller Verified" instead of a bare checkmark. PRD §12/§18 still leaves "who gets a badge, on what evidence" undecided, so every gate below is a documented judgment call, not a spec requirement — schema/migration and eligibility/application design were already sketched (uncommitted) when this pass started; this pass applied the migration and built `lib/verification/{eligibility,applications,serialize}.ts` plus the API routes, backend only (no UI, no `VerifiedBadge` changes).

- **Eligibility gates are deliberately light** (`lib/verification/eligibility.ts`): IDENTITY and OFFICIAL are reachable by anyone (evidence-only review); SELLER/BUSINESS require ≥1 `PUBLISHED` product; CREATOR requires ≥1 published RELEASE/BEAT; FANBASE requires the applicant to be the target group's creator. These only gate whether the *form* is reachable — a reviewer can still reject a technically-eligible application on the actual evidence.
- **"Valid sale" = a live (non-revoked) `Entitlement`** on the creator's own product, not PAID-order/OrderItem counting — `Entitlement` is already the single source of truth for "this sale still counts" (created once at payment/gift-claim, revoked once on refund), and unlike `StockPolicy.sold` it exists for uncapped products too.
- **50-valid-sales milestone (`User.salesMilestoneNotifiedAt`) is a one-time nudge, not a SELLER eligibility gate** — SELLER only requires one published listing. 50 is a judgment call (no PRD number) for "this is an ongoing seller," exposed via `GET/POST /api/verification/sales-milestone` for a future client-side modal.
- **At most one non-terminal application per type per user**, enforced in `lib/verification/applications.ts` rather than a DB constraint (Postgres partial unique indexes aren't expressible in Prisma's schema language without raw SQL) — matches the schema comment's own stated approach.
- **Per-type required fields/documents to submit** are a judgment call, not a legal checklist: IDENTITY needs legal name/DOB/country/doc number + ID_FRONT/SELFIE uploads; BUSINESS additionally needs BUSINESS_REGISTRATION; OFFICIAL needs `categoryData` evidence + SUPPORTING_EVIDENCE; SELLER/CREATOR need only legal name/country (their published catalog is the evidence); FANBASE needs only `categoryData` (the group-ownership gate above is the real check).
- **FANBASE is the one type where applicant-requested type ≠ granted badge** — the applicant always requests FANBASE, but the reviewer chooses `OFFICIAL_FANBASE` vs `RECOGNIZED_COMMUNITY` at approval time (`PATCH .../approve` requires `fanbaseBadge` only for this type); every other type maps 1:1 (SELLER → SELLER_VERIFIED, etc).
- **Suspend/revoke strip the specific badge but never touch `isVerified`** — matches the schema comment's explicit contract (something else, e.g. the old moderator toggle, may still want the checkmark on). Revoking straight from SUSPENDED skips a second badge-removal (already stripped at suspend time); revoking from APPROVED removes it. Both the status transition and the badge mutation run inside one `$transaction`.
- **Documents are private-only** — `verification/{userId}/{applicationId}/{documentType}/{uuid}.ext` keys, resolved exclusively via `presignDownload` (never `publicUrlFor`), matching the existing PRD §16 pattern for paid audio. A document row is created immediately on presign (before the client's PUT actually lands) for the audit trail; no polling/confirmation step, consistent with this project's "simpler to develop, synchronous" convention elsewhere.
- **Status-change notifications use `sendPushToUser`, not `createNotification`** — the bell/`Notification` model is explicitly scoped to transactional/money events only (see its own doc comment); verification approvals/rejections/etc. are exactly the kind of non-money event push already handles for follows/likes/comments.
- **Verified via a real Prisma-backed flow, not just typechecking**: seeded throwaway users/product/group, ran create → submit-without-required-fields (blocked) → fill required fields → submit → approve → suspend → revoke through the actual `lib/verification/applications.ts` functions against the local dev DB, plus the FANBASE non-owner rejection and reviewer-chosen-badge paths, confirmed the audit log recorded every transition in order, then deleted the seeded rows. All API routes were also hit unauthenticated to confirm they compile and 401 correctly rather than 500ing.

## Verification system — UI, expiry sweep, sales-milestone nudge (2026-08-24)

Continuation of the pass above: the applicant-facing form, the admin review queue, and the two pieces explicitly deferred last time.

- **`/verification`** (new top-level route, not folded into Profile/Settings — there's no dedicated settings route in this app, and this is substantial enough to be its own page) — lists the user's own applications with status, a type picker (with an owned-group dropdown for FANBASE, sourced from the existing member-groups `GET /api/groups` filtered client-side to `creatorId === appUser.id` — no new endpoint needed), and a single form component reused for both creating and continuing a draft or a `MORE_INFO_REQUIRED` application.
- **`categoryData`'s free-form JSON is surfaced as one "tell us more" textarea** (`{ notes: string }`) rather than bespoke per-type fields (business registration number, creator social links, etc., as separate inputs) — the schema keeps this column freeform specifically to avoid per-type columns, and inventing a distinct field set per type without a spec for what evidence each actually needs would be scope creep beyond what PRD §12/§18 asks for. Revisit if a real type ends up needing structured evidence.
- **Moderation gets a `VerificationQueuePanel`**, matching this file's existing inline-panel convention (`ManageModeratorsPanel`, `VerifyCreatorPanel`, `VerifyGroupPanel` are all defined directly in `moderation/page.tsx`, not split into `components/`) rather than the older blunt toggles — kept side by side since the old ones still work standalone (backward compatibility, per the schema comment). A status filter (open / approved / all) exists because suspend/revoke only make sense against `APPROVED` rows, which the default "open" queue never shows.
- **`VerifiedBadge` takes an optional `badgeType`** and swaps its `aria-label`/`<title>` tooltip (e.g. "Verified seller" vs a bare "Verified") — the glyph itself doesn't change per badge type; inventing seven distinct icons wasn't worth it for what's currently just a tooltip difference.
- **IDENTITY is the only type that ever gets `expiresAt`** — set at approval time to the application's own `documentExpiresAt` (the government ID's expiry). Every other type (SELLER/CREATOR/BUSINESS/OFFICIAL/FANBASE) has no natural "this expires on X" signal, so they stay indefinite until a moderator explicitly suspends/revokes — a judgment call, not a PRD requirement.
- **The expiry sweep extends the existing `sweep-holds` cron** rather than adding a fourth `vercel.json` cron entry — same reasoning as the gift-expiry sweep already living there (see the Verification/Collections/Gifting/Fanbase entry above): one unused Hobby-plan cron slot isn't worth spending on something this infrequent, and `sweep-holds` is already the house for "time passed, silently transition a status" jobs.
- **`VerificationAuditLog.actorId` is a required `User` FK with no system-actor concept anywhere in this schema.** Rather than add a nullable column or a synthetic system user just for one audit-log row per expiry, the system-triggered `VERIFICATION_EXPIRED` entry is attributed to the applicant's own id.
- **The 50-sales milestone nudge lives on `/profile`**, the app's de facto hub page (reached on effectively every session) — checked once on mount via the existing `GET /api/verification/sales-milestone`, shown as a `SalesMilestoneSheet` bottom sheet (same shape as `ReportSheet`). Both "Not now" and "Apply now" call the mark-as-seen `POST` so it never re-triggers, matching the field's own one-shot contract.
- **Verified end-to-end against the real local DB again**: an IDENTITY application approved with a `documentExpiresAt` in the past, confirming approval correctly copies it to `expiresAt`, the sweep transitions it to `EXPIRED` and strips the badge (while leaving `isVerified` alone) but does nothing on a second run, and a parallel SELLER approval confirms non-IDENTITY types never get an `expiresAt`. Also hit the real `/api/internal/sweep-holds` route with the actual `CRON_SECRET` (401 without it, 200 with it, returns the new `verificationsExpired` count) rather than only testing the underlying function. `tsc`, `eslint`, and `vitest` all clean; `/verification`, `/moderation`, and `/profile` all confirmed to compile and render (200) through the dev server.

## Delete-with-confirm on every upload, app-wide (2026-08-24)

Explicit ask: every upload surface should let its own uploader permanently delete what they uploaded, gated behind a confirm prompt naming the permanence. Extended past verification documents (already had this) to every other upload surface in the app.

- **Persisted, revisitable-later uploads got a real `DELETE` API + confirm**: `DELETE /api/me/avatar`, `DELETE /api/me/cover` (null the field), and `DELETE /api/groups/[id]/photo` (admin-gated, mirrors the existing `POST`'s admin check) — none previously existed. `PhotoActionSheet` (avatar/cover) gained a "Delete photo" option; the group photo's small circular avatar button gained a tiny "×" badge, since that surface has no action-sheet to add an option to. Confirm copy: "This … will be permanently deleted. Continue?", matching `ModerationPage`'s existing `window.confirm` convention for the takedown action.
- **Not-yet-persisted, pre-submit local state (draft images/tracks in `publish/*`, onboarding avatar, post/chat composer attachments) also got delete-with-confirm**, even though nothing is actually saved to a revisitable record yet — explicit ask covered "every upload," and removing an already-uploaded-to-R2 file (even pre-publish) is exactly the kind of accidental-tap-loses-my-selection moment a confirm is for. `TrackUploader`'s and `ChatComposer`'s pre-existing bare "Remove" buttons gained the same confirm; single-image pickers (merch/music/beat/event artwork, merch gallery, onboarding avatar) gained a small "×" overlay button (matching `PostComposer`'s pre-existing pattern, which already had one) rather than a text link, for visual consistency across the publish flows.
- **R2 objects themselves are never explicitly deleted on these paths** — same "orphaned key is harmless since nothing references it" reasoning as the verification-documents pass; only the DB pointer (or local draft state) is cleared.
- **Verified via `tsc`, `eslint` (both clean across every touched file), and hitting the new `DELETE` routes plus every touched page through the dev server** (401 unauthenticated as expected, 200 render on every page) — the local dev Postgres instance was intermittently unreachable this session (unrelated to these changes, which don't touch any DB-dependent test), so this pass leans on route-compile + type-safety verification rather than a live-DB flow test the way earlier verification-system passes did.

## Owner edit, time-boxed to 48 hours after publish (2026-08-24)

Explicit ask: every uploaded product needs a real, visible edit feature for its owner, available only within 48 hours of publishing. `PATCH /api/{releases,beats,merch}/[id]` and `PATCH /api/events/[id]` already existed (description/price/cap-lowering, venue for events) but had **zero UI** anywhere in the app — this pass built the UI and added the time box.

- **`lib/editWindow.ts`** is the single `isWithinEditWindow(publishedAt)` gate, imported by both the server routes (enforcement) and the client catalog pages (hiding the Edit affordance once it's moot) — one 48-hour constant, never duplicated, can't drift between what the button shows and what the server actually accepts.
- **The window applies to the whole PATCH endpoint, not just new fields** — including the pre-existing, previously-UI-less price/cap-lowering capability. That capability had no UI and so no one could have been relying on unlimited-time editing; time-boxing it now to match the explicit ask is simpler than carving out an exception for fields that happened to exist before this pass.
- **`title` is now patchable on all four types** (previously only description/price/cap/shippingFee/venue) — the most obvious thing an "edit feature" needs (fixing a typo) and has no PRD invariant against it, unlike price/cap. Artwork, audio, and tracks stay **not** editable, on purpose — those are what a buyer already owns a copy of; changing them post-sale would be a re-publish masquerading as an edit, not what this ask is for.
- **Ticket tiers gained `name` editing** on the same existing `PATCH /api/events/[id]/tiers/[tierId]` route, gated by that *tier's own* `Product.publishedAt` (not the parent event's) — tiers are independent `Product` rows created atomically with the event today, but the per-tier gate is the more correct one if that ever changes.
- **UI pattern**: an inline "Edit" toggle next to the existing "Delete" button on every catalog row (`/profile/catalog`, `.../beats`, `.../merch`), expanding a form below the row — mirrors the exact `TierEditor`/"Manage" pattern the Events catalog already had for tier price/cap, extended to cover title/description/price/cap for every type and to a new event-level `EventEditor` (title/description/venue) inside the same "Manage" panel. Past the 48-hour mark the Edit button (or, for tiers, the editable inputs) simply doesn't render — a plain sold/price summary and an "editing closed" note take its place.
- **Verified via `tsc`/`eslint` (clean across every touched file), `vitest` (11/11), hitting every changed route/page through the dev server, and a live-DB check of the actual boundary**: seeded a product published now and one backdated 49 hours, confirmed `isWithinEditWindow` reads true/false correctly for each and that a real Prisma title update persists for the in-window one.

## Bug fix: settings toggle knob, and BottomNav floating over the keyboard in a Fanbase chat (2026-08-25)

- **Settings toggle knob was overflowing its track — two separate bugs, found in two passes.** First pass fixed the translate *magnitude* (`translate-x-[22px]` → `translate-x-5`, accounting for the track's 1px border eating into its 44px `w-11` width) and swapped the knob to white when off / red when on (explicit request, superseding the earlier "dot is always red" call). That alone didn't fix it — a screenshot of the still-broken result led to the real, dominant bug: the dot `<span>` had no explicit `left`, and browsers resolve an absolutely-positioned element's "auto" static position using its pre-absolute display type — a `<span>` is naturally `inline`, and `<button>`'s UA-default `text-align: center` was centering that hypothetical inline box, landing the dot's *starting* position well right of center before `translate-x` even applied (confirmed by inspecting the actual compiled Tailwind v4 output and matched CSS rules in a live browser, not just computed by hand — a synthetic test with hand-written CSS had wrongly suggested the first fix alone was sufficient, because it didn't reproduce the button's default `text-align`). Fix: added `left-0` to anchor the static position to a known 0, making the already-correct `translate-x-0.5`/`translate-x-5` move it a predictable, in-bounds amount from the track's real left edge. Checked every other `translate-x-*` usage in the app (all fixed `translate-x-[1px]` nudges on static, non-absolute SVG icons) — none share this bug.
- **`BottomNav` (the five-tab bar) now hides on a Fanbase group's own chat view (`/groups/[id]`), not just the already-excluded `/login`/`/onboarding`/`/u/[handle]`.** Root cause: the h-dvh keyboard-avoidance layout (see the earlier "Fix keyboard-avoidance..." entry) reflows the *entire* bottom flex-column stack — including `BottomNav` — up above the keyboard, not just the page's own `ChatComposer`. In a full-height chat view that meant two bars stacked directly above the keyboard (the tab bar *and* the composer) instead of just the composer sitting right above it. No back button was added for this route — `/u/[handle]` (the other nav-less surface) has never had one either and relies on browser/gesture back, so this matches existing convention rather than inventing a new affordance. **Superseded by the entry below** — a blanket route-based hide meant the tab bar never showed on the Fanbase chat view even when the keyboard wasn't open, which wasn't the actual ask.

## Refined: BottomNav/MiniPlayer hide only while the keyboard is actually open in a Fanbase chat (2026-08-25)

Explicit correction to the pass above: both bars should stay visible on `/groups/[id]` normally, and hide *only* while the on-screen keyboard is open — not for the whole time you're on that route.

- **`lib/useKeyboardOpen.ts`**: a shared hook using the `visualViewport` resize event — the visual viewport shrinks when a mobile keyboard opens (unlike the layout viewport/`window.innerHeight`), so `window.innerHeight - visualViewport.height` crossing a 150px threshold is the standard cross-browser way to detect this. 150px is comfortably below any real keyboard's height and comfortably above ordinary viewport jitter (address bar show/hide).
- **Both `BottomNav` and `MiniPlayer` now hide only when `pathname.startsWith("/groups/") && keyboardOpen`** — visible by default on the Fanbase chat view like anywhere else, hidden only for the duration the keyboard covers the screen, back the instant the field blurs. Scoped to `/groups/` specifically, not applied globally — elsewhere in the app the original h-dvh float-up-with-keyboard behavior is unchanged, since only the Fanbase chat view was reported as actually having the "two bars stacked above the keyboard" problem.
- **Playback is unaffected by hiding `MiniPlayer`** — the actual `<audio>` element and all playback state live in `PlayerProvider`, mounted once at the app root independent of what `MiniPlayer` (a purely presentational view of that state) renders. Hiding it is a conditional `return null` in `MiniPlayer` alone; nothing about the player's own lifecycle changes.
- **Verified the hook's reactivity directly in a live browser**, not just by reading the code: since a real on-screen keyboard can't be triggered from this environment, overrode `window.visualViewport`'s `height` getter (via `Object.defineProperty`, since it's normally read-only) to simulate a 300px keyboard, dispatched a real `resize` event on it, and confirmed the hook's state — and both components' conditional render logic — flipped correctly in each direction (open → hidden, close → visible again). `tsc`/`eslint` clean; `/groups/[id]` and an unrelated route both confirmed to still compile and render.

## Superseded again: BottomNav is now truly `position: fixed`, no JS keyboard detection (2026-08-25)

Explicit correction to the two passes above: "the bottomnav bar should be fixed on the bottom, [the] keyboard should float on top of it" — i.e. it shouldn't move at all, ever, and the keyboard should simply overlay/cover it, rather than the bar hiding itself or reflowing upward. A user screenshot of the still-live "everything floats up together" behavior (composer, MiniPlayer, and BottomNav all squeezed above the keyboard in a stack, with the OS's own keyboard-accessory toolbar visible above the actual keys) confirmed the prior JS-hide approach hadn't actually changed that stacked-reflow feel — the bar was disappearing rather than just staying put.

- **`BottomNav`'s `<nav>` changed from `sticky bottom-0` (a flex child of the h-dvh-reflowing column) to `fixed inset-x-0 bottom-0`, pinned to the real/static viewport bottom** — deliberately *outside* the dvh reflow the root layout still uses for everything else. This is the well-known default mobile-browser behavior for fixed-positioned elements (the exact behavior the original "Fix keyboard-avoidance" pass was written to escape, for the whole bottom stack) — reintroducing it specifically for `BottomNav` means a keyboard opening anywhere in the app now overlays on top of it instead of pushing it up, with no JS involved.
- **The `useKeyboardOpen`-based conditional hide is removed from `BottomNav` entirely** — redundant now that it physically can't be pushed around by the keyboard. The hook itself stays (still used by `MiniPlayer`, unchanged by this pass — only `BottomNav` was in scope for this ask).
- **A `<div aria-hidden className="h-16 shrink-0" />` spacer replaces the space `BottomNav` used to occupy in normal flow**, sized to its actual measured rendered height (~64px at 16px root font-size, confirmed via `getBoundingClientRect` in a live browser) — without it, `MiniPlayer` and the scrollable content area would have nothing reserving room for the now out-of-flow fixed bar and would render underneath it. If `BottomNav`'s own content/padding ever changes, this needs remeasuring alongside it.
- **`ChatComposer`'s float-above-keyboard behavior is untouched** — it was never part of `BottomNav`; it still lives inside the h-dvh-reflowing content column (`app/layout.tsx`'s `{children}` slot), which keeps working exactly as the original "Fix keyboard-avoidance" pass intended. Only the separate bottom-chrome stack (specifically `BottomNav`) changed.
- **Verified in a live browser**: `getComputedStyle(nav).position === "fixed"` and its bottom edge sits flush with `window.innerHeight` (not just visually, confirmed via `getBoundingClientRect`) on a real page; screenshotted to confirm no gap or content overlap where the spacer meets the fixed bar. Could not trigger a real on-screen keyboard in this environment to screenshot the "covered by keyboard" end state directly — this relies on the well-documented, long-standing default `position: fixed` + mobile-keyboard behavior rather than a live keyboard trial. `tsc`/`eslint`/`vitest` (11/11) all clean.

## Second checkout processor: Monnify, buyer-selectable at checkout (2026-08-25)

Explicit ask, clarified via follow-up: the buyer picks between Flutterwave and Monnify at checkout (not a per-creator setting, not Monnify replacing Flutterwave). `Payment.processor` already existed in the schema (`@default("flutterwave")`, unused until now) — no migration needed, just actually wiring it up.

- **`lib/monnify.ts`** mirrors `lib/flutterwave.ts`'s shape deliberately (`initializePayment`/`verifyTransaction`/`initiateRefund`, amounts in Naira not kobo) so the two read side by side. Monnify's access token is short-lived (~1hr) and meant to be reused, not fetched per call, so it's cached in module scope with a 60s expiry margin. `initiateRefund`'s `amountKobo` is **required**, unlike Flutterwave's optional one — Monnify's refund API isn't confirmed to default to a full refund when the amount is omitted, and the one real caller (the takedown flow) always has the amount on hand anyway, so there's no reason to guess.
- **Extracted `lib/commerce/confirmPayment.ts`'s `finalizePayment()`** out of the Flutterwave webhook — idempotency claim, order→PAID, gift/entitlement creation, stock confirm, ledger record, confirmation email, buyer/seller notifications — none of which is processor-specific. Both `app/api/webhooks/flutterwave/route.ts` (updated) and the new `app/api/webhooks/monnify/route.ts` now do only their own signature check + authoritative `verifyTransaction` call, then hand off to the same shared function — a second processor's webhook couldn't duplicate ~90 lines of business logic without this.
- **Monnify's webhook signature is HMAC-SHA512 of the raw body** (header `monnify-signature`), genuinely different from Flutterwave's `verif-hash` (a plain shared-secret string, not a body HMAC) — `req.text()` first, verified via `timingSafeEqual`, *then* `JSON.parse`, same raw-body-before-parsing discipline any HMAC check needs.
- **`POST /api/orders` takes a `gateway: "flutterwave" | "monnify"` field** (default `"flutterwave"`, so older/未-updated clients keep working), branching which `initializePayment` runs and stamping `Payment.processor` accordingly. Free (₦0) orders are untouched — they already bypass every processor entirely, so `gateway` is simply ignored for those.
- **The takedown refund flow (`app/api/reports/[id]/route.ts`) now branches on `payment.processor`** before calling `initiateRefund` — previously hardcoded to Flutterwave, which would have silently tried to refund a Monnify-paid order through the wrong processor.
- **Buyer-facing picker**: `lib/useGatewayCheckout.ts` (a `pickGateway(): Promise<Gateway>` a component `await`s before calling `POST /api/orders`, resolved by `GatewayPickerSheet`'s tap, **rejected with `GatewayPickerCancelled`** if the sheet is dismissed without a choice — without that rejection, a cancelled sheet would leave the awaiting `handleBuy`/`handleGift` pending forever, spinner stuck) + `components/checkout/GatewayPickerSheet.tsx` (same bottom-sheet shape as `ReportSheet`). Wired into all four purchase entry points — `PurchaseAndPlayer` (Release), `BeatPurchaseAndPlayer`, `MerchPurchaseForm`, `EventTierPicker` — each `catch`­ing `GatewayPickerCancelled` specifically so a deliberate cancel doesn't surface as "something went wrong". The picker is skipped entirely for a ₦0 item (no processor involved, same as above).
- **Not verified against Monnify's real API** — no sandbox credentials were available in this environment (`MONNIFY_API_KEY`/`MONNIFY_SECRET_KEY`/`MONNIFY_CONTRACT_CODE` are unset placeholders in `.env.local.example`, same convention as every other secret there). `lib/monnify.ts` is written to match Monnify's documented public API contract, and the *shared, processor-agnostic* logic it feeds into (`finalizePayment`) was verified live against the local dev DB instead: seeded a real order/payment with `processor: "monnify"`, called `finalizePayment` directly with a fabricated verified-success result, confirmed the order went `PAID`, the entitlement was granted, stock incremented, the ledger entry was recorded exactly once even when the same call was repeated (idempotency), and that a fabricated verified-failure result correctly released the hold and marked the order `FAILED` without granting anything. `tsc`/`eslint` clean across every touched/new file; `vitest` 11/11; every new/changed route and all four product detail pages confirmed to compile and respond correctly (401 on missing signature/auth, 200 render) through the dev server.

## Bug fix: lock-screen "play" could silently no-op instead of resuming (2026-08-26)

Explicit bug report: pausing, then trying to resume playback "outside the app or locked screen" (i.e. via the OS media-control surface, not the in-app buttons) sometimes just didn't — the song stayed stopped.

- **Root cause**: `navigator.mediaSession.setActionHandler("play", togglePlay)` and `("pause", togglePlay)` were both bound to the *same* `isPlaying`-branching toggle function. That function decides what to do by reading its own closed-over `isPlaying` — but the OS already knows, unambiguously, which button was actually pressed; re-deriving intent from potentially-stale internal state is redundant and, here, the actual bug. If that closure hadn't yet re-rendered with a fresh `isPlaying` value by the time a lock-screen tap landed (plausible while backgrounded/locked, where JS scheduling isn't guaranteed prompt), pressing "play" could evaluate the stale `isPlaying === true` branch and call `audio.pause()` on already-paused media — a silent no-op that looks exactly like the song refusing to resume.
- **Fix**: split `togglePlay` into two unconditional, idempotent halves — `resumePlayback()` (the old "not playing" branch: stale-URL re-fetch via `needsFreshUrlRef`, `audio.play().catch()` fallback) and `pausePlayback()` (`audio.pause()`). The Media Session `"play"`/`"pause"` handlers now bind directly to these instead of the shared toggle — the OS's own button choice drives the action, not a re-derived guess. `togglePlay` itself is unchanged for the in-app single-button UI (`MiniPlayer`/`ExpandedPlayer`), just now implemented as `isPlaying ? pausePlayback() : resumePlayback()`.
- **Side benefit, not just a correctness fix**: `pausePlayback` has no dependencies (stable across every render) and `resumePlayback` only changes identity when the current track/queue does — not on every play/pause toggle the way the old shared function did. The Media Session registration `useEffect` now re-runs far less often, shrinking whatever staleness window remains even further.
- **Verified via `tsc`/`eslint`** (clean) and confirmed every existing in-app call site (`MiniPlayer`, `ExpandedPlayer`, `BeatPurchaseAndPlayer`, the Library page) still calls the unchanged `togglePlay()` with no arguments, so none of them needed updating. Could not verify the actual lock-screen/background-tab race live — this environment has no OS lock screen or a way to genuinely background/throttle the page — so this is a code-level fix for a well-understood, documented class of Media Session bug (MDN's own examples register independent `"play"`/`"pause"` handlers, not a shared toggle, for exactly this reason), not a live-reproduced-then-fixed bug.

## Moderation dashboard: platform-growth stats, its own login gate, moderator emails hidden (2026-08-26)

Three explicit asks in one pass: user-growth tracking on the dashboard, a moderator-specific login reachable directly at `/moderation` instead of the consumer flow, and (a quick follow-up once the first two were live) hiding moderator email addresses from the moderator-management list.

- **`GET /api/admin/stats`** (moderator-gated): total/active/deleted users, new signups in the last 24h/7d/30d, total moderators, and "creators" (users with ≥1 non-draft product — the same bar `/api/analytics` implicitly uses; there's no separate role flag). Rendered as plain stat tiles in a new `PlatformStatsPanel`, no chart — same call the creator-facing analytics pass made ("the PRD requires the metrics, not a visualization").
- **`/moderation` now owns its own auth gate** instead of sharing the consumer login/onboarding redirect every other `(app)` route uses. `app/(app)/layout.tsx` gained a `SELF_GATED` set (distinct from `PUBLIC_EXACT` — a signed-out visitor still can't see the dashboard, this route just handles its *own* gate instead of bouncing to `/login`) so `ModerationPage` itself renders a lighter `ModeratorLoginForm` (email/password only — no Google sign-in, no "create an account" link, since moderator accounts are internal staff provisioned directly in the DB per PRD §3, never self-serve) when signed out, the existing "you don't have access" message for a signed-in non-moderator, and the dashboard for an actual moderator.
- **Moderator email addresses removed from `ManageModeratorsPanel`'s list** (and from `GET /api/admin/moderators`'s response entirely, not just hidden client-side — no reason to ship data the UI no longer displays) — explicit follow-up ask. The panel itself was already `requireSuperModerator`-gated both in the UI (only rendered when `appUser.isSuperModerator`) and at the API layer, so a *non*-super moderator could never reach this list either way; removing the email is a privacy tightening for whoever *can* see it (other super-moderators), not a new access-control boundary.
- **Verified via `tsc`/`eslint`** (clean across every touched/new file) and hit every changed/new route through the dev server: `/moderation` now returns 200 (renders the login form) instead of redirecting when signed out, and `/api/admin/stats` / `/api/admin/moderators` both 401 without auth as expected. The local dev Postgres was unreachable for most of this session's remaining time despite repeated restarts — unrelated to these changes (simple `count()` queries and client-side auth-gate branching, not new business logic), so this pass relies on `tsc`/route-compile verification rather than a live-DB flow test the way earlier passes with actual new business logic did.
- **Verified live in production** (the deployed commit, not local dev): loaded `/moderation` while signed in — Platform Growth tiles showed real numbers, the moderator list showed no email. Then explicitly signed the browser out (via the real Log Out button, with the user's go-ahead) and reloaded `/moderation` to confirm the *new* login gate specifically — it rendered the dedicated "Moderator Access" form at that URL, not a bounce to `/login`. No credentials were entered (that's the account owner's step, not something this session does); the user logged back in afterward on their own.

## Growth chart: Day/Week/Month/Year trend with signed, colored deltas (2026-08-26)

Two asks: simplify the "New, 7d"/"New, 30d" stat-tile labels, and give the growth section an actual chart — day/week/month/year, with color and a sign showing which way it's moving. Loaded the `dataviz` skill before writing any chart code, per its own trigger rule.

- **Labels**: "New, 7d" → "New this week", "New, 30d" → "New this month" ("New today" was already plain).
- **Bars, not a line** — each bucket is a discrete period count (a magnitude comparison across ordered categories), not a continuous measurement, so this reads as "compare magnitude" in the skill's form table, not "trend over time." Single series, one hue: `--red`, this app's own existing brand accent, not an invented "chart color."
- **The up/down pair was chosen by the validator, not by eye.** `--green` + `--red-soft` (the codebase's existing error-text color) FAILs the skill's lightness-band check for a dark-mode pair; `--green` + `--red` (the codebase's own primary brand red) passes every check but the CVD floor band (6–8, legal only with secondary encoding) — satisfied here because the color is never alone: a ▲/▼ glyph and a signed number ride along with it (`node scripts/validate_palette.js "#3f9e6b,#e11d2e" --mode dark --surface "#0a0a0b"`).
- **`GET /api/admin/stats` gained a `growth` field** — `{ day, week, month, year }`, each an array of `{ label, count }`. Bucketing happens in JS, not SQL `GROUP BY`/`date_trunc` (a single unbounded fetch of every `User.createdAt` is simpler than four separate queries and just as cheap at this scale — tens to low-thousands of rows). Day/week are trailing windows anchored to "now" (sidesteps picking a week-start convention); month/year are true calendar periods, since a rolling "30-day month" bucket would misrepresent February.
- **The delta headline compares the most recent bucket to the one before it** — for "day" that's literally today vs. yesterday, which is the standard (if slightly apples-to-oranges — "today" is a partial period) convention every stat-tile-with-a-delta dashboard uses; not specially normalized for partial-period length, matching the scope of an internal tool rather than a public-facing metric.
- **Bar width is capped at 24px** (`max-w-6`) **but grows via `flex-1` to fill the card down to that cap** — 30 daily bars render thin and packed, while 2–3 yearly bars stay a sane width instead of stretching to fill the whole card edge-to-edge (verified visually — see below).
- **Hover/focus updates the headline number and label to the hovered bucket** (with the delta badge hidden, since a delta only means something for the *current* period) rather than a floating tooltip — chosen over a positioned tooltip specifically because this app is mobile-first with narrow chart cards, where a floating tooltip risks overflowing the viewport; the headline-readout pattern satisfies the same "every value reachable via hover *and* focus" requirement without that risk.
- **Rendered and looked at it, per the skill's step 7** — the validator checks color, not layout. Built a throwaway `/chart-test` route (removed after) that stubbed `window.fetch` to serve fabricated bucket data with no auth/DB needed, screenshotted all four granularities: 30 daily bars pack cleanly with no collision, the 3-bar "Years" view correctly stays capped-width instead of stretching, hovering a non-current bar swaps the headline to that bucket's value/label and correctly suppresses the delta, and the down-example (Day 30 vs. Day 29, artificially seeded to dip) rendered red/▼ while the up-example (2026 vs. 2025) rendered green/▲ — confirming the signed color logic both directions, not just the happy path. `tsc`/`eslint` clean.

## Moderator step-up 2FA: email OTP + 60s inactivity bounce (2026-08-26)

Explicit ask: moderators keep signing in with their normal email/password (the existing `ModeratorLoginForm`, unchanged), but reaching `/moderation` now additionally requires a one-time code emailed to them, and any 60 seconds of inactivity on the dashboard bounces them back out to the normal user experience — requiring a fresh code to get back in. This sits on top of the existing self-gated `/moderation` login from the prior pass, not a replacement for it.

- **New `ModeratorOtpCode` model** (`userId`, `codeHash`, `attempts`, `expiresAt`, `consumedAt`) — the code itself is never stored in plaintext, only a SHA-256 hash, matching how the rest of the app treats anything secret-shaped. Hand-written migration (`20260826120000_moderator_otp`) applied via `migrate deploy`, same shadow-DB workaround as every other migration this session (a pre-existing, unrelated `GroupVisibility` conflict in an old baseline migration breaks `migrate dev`'s shadow DB).
- **`lib/auth/moderatorOtp.ts`**: `issueModeratorOtp(userId)` generates a random 6-digit code, invalidates any prior *unconsumed* code for that user first (so only one code is ever live — requesting a new code silently retires the old one rather than letting either work), stores its hash with a 10-minute expiry. `verifyModeratorOtp(userId, code)` compares via `crypto.timingSafeEqual` (not `===`, to avoid a timing side-channel on the hash comparison), increments `attempts` on a wrong guess and locks the code out entirely after 5 attempts (further attempts fail even with the right code — a fresh `issueModeratorOtp` call is the only way back in), and marks `consumedAt` on success so the same code can't be replayed.
- **Delivery reuses the existing `lib/email.ts` primitives** (`sendEmail`/`escapeHtml`) — `sendModeratorOtpEmail` matches the raw-HTML-string style of every other template in that file rather than introducing a new templating approach for one email.
- **Two new moderator-gated routes** (`requireModerator`, the same guard every other `/api/admin/*` route already uses): `POST /api/admin/otp/request` issues+emails a code, `POST /api/admin/otp/verify` (zod-validated 6-digit `code`) checks it and returns 401 on any failure (wrong code, expired, locked out, or none pending) without distinguishing which — no reason to hand an attacker more signal than "no").
- **The 60-second inactivity window is client-side and per-tab, deliberately** — `lib/useModeratorSession.ts`'s `useModeratorSession()` hook keeps a "valid until" timestamp in `sessionStorage` (survives a reload *within* an active window without re-prompting, but a genuinely stale or reopened tab starts clean) extended by real DOM activity listeners (`mousemove`, `mousedown`, `keydown`, `touchstart`, `scroll`, `wheel`, all `passive`). Lapsing calls `router.push("/discover")` and clears the stored timestamp — exactly the "bounce to normal user dashboard, require a fresh OTP" behavior asked for. This is a UX/session-hygiene control, not the actual access-control boundary — `requireModerator` on every `/api/admin/*` route (unchanged, still checks `isModerator` server-side on every request) is what actually gates data; the 60s timer only gates the *page*, so a lapsed session can't be reasoned about as "the API trusts the timer."
- **`app/(app)/moderation/page.tsx` gained an OTP gate** between the existing "not a moderator" check and the dashboard itself: `!otpVerified` renders a new `ModeratorOtpForm` (masked email shown via a new `maskEmail()` helper, e.g. `ch***71@gmail.com`, auto-sends a code on mount, 6-digit input, "Resend code" action) instead of the dashboard; `markVerified()` from the hook flips it open for the next 60s of activity. `AppUser` gained an `email` field (previously absent from the client-side user shape) since the form needs it to render the masked address.
- **The mount `useEffect`'s initial send duplicates the `requestCode` function's body as an inline `initialSend`** rather than calling the `useCallback`-memoized version directly — same `react-hooks/set-state-in-effect` lint fix applied at least three times elsewhere this session (verification page, `BottomNav`); calling a memoized function from inside an effect that also sets state trips the rule, and the established fix in this codebase is a plain function declared inside the effect body, not restructuring the memoization.
- **Verified with a live 13-assertion test against the local dev DB** (`scratch-otp-flow.ts`, deleted after): code format (exactly 6 digits), hash-not-plaintext storage, wrong-code rejection with attempt counting, correct-code acceptance and consumption, replay prevention (a consumed code fails on reuse), rotation (issuing a second code invalidates the first), expiry enforcement, and lockout after 5 wrong attempts (a 6th attempt fails even with the right code) — all 13 passed. Re-ran the shared `inspect.ts` cleanup check afterward to confirm the dev DB was left empty.
- **`tsc --noEmit` and `eslint` clean** across every touched/new file; `vitest` 11/11, no regressions. Smoke-tested the new surfaces through the dev server: `/moderation` still 200s (renders the login/OTP gate chain rather than redirecting), `POST /api/admin/otp/request` and `POST /api/admin/otp/verify` both 401 unauthenticated, matching every other `/api/admin/*` route's behavior.
- **Not committed** — awaiting the user's explicit go-ahead, same standing rule followed after every other feature this session.

## Moderator user directory (2026-08-26)

Explicit ask, immediately after the OTP pass above: moderators should be able to see the list of users in their dashboard.

- **`GET /api/admin/users`** (`requireModerator`-gated): a searchable (`q` — matches handle/displayName/email, case-insensitive substring), paginated (`page`, fixed 25/page) directory. `deletedAt: null` by default; `includeDeleted=1` opts into seeing soft-deleted accounts too (useful alongside the existing `RestoreAccountPanel`, which needs a handle to act on). Offset pagination (`skip`/`take`), not a cursor — matches every other admin panel in this file, which are all plain lists; safe at the "tens to low-thousands of users" scale the `/api/admin/stats` comment already establishes for this app.
- **Emails are shown**, deliberately unlike `ManageModeratorsPanel`'s email removal from the prior privacy pass — that change was about *peer* moderators not seeing each other's personal email; this is moderators looking up *ordinary users*, where an email is normal support/lookup context, not a privacy boundary between staff.
- **`listingCount` comes from a Prisma relation count** (`_count.products`) in the same query, not a second round-trip — cheap and gives a moderator an at-a-glance signal (a user with zero listings vs. an active seller) without a separate "is this person a creator" query.
- **`UsersListPanel`** (new, in `app/(app)/moderation/page.tsx`): debounced search input (300ms, matches the existing `/search` page's `setTimeout`-in-`useEffect` pattern, including its established `react-hooks/set-state-in-effect` eslint-disable), an "include deleted" checkbox, a Previous/Next pager, and a "View" link to each user's `/u/[handle]` profile. Changing the query or the checkbox resets to page 1 directly in the `onChange` handlers rather than a separate reactive effect — the first draft used a `useEffect([q, includeDeleted]) { setPage(1) }` effect and tripped the same `react-hooks/set-state-in-effect` lint rule seen repeatedly this session; unlike the established "duplicate the logic inside the effect" fix used elsewhere, here the state update has no async work to wait on, so folding it directly into the existing `onChange` handlers is simpler and avoids the extra effect entirely.
- **Verified live against the local dev DB**: seeded 4 scratch users (a moderator, a verified user, a plain user, a soft-deleted user) plus one published product, ran 10 assertions directly against the same Prisma query shape the route uses — default list excludes the deleted account, `includeDeleted` includes it, case-insensitive substring search works across handle/displayName/email independently, `listingCount` is correct (1 vs. 0), and paginated pages (`skip`/`take`) don't overlap and together cover all 4 scratch rows. All 10 passed; scratch data deleted after and the dev DB reconfirmed empty.
- **`tsc --noEmit` and `eslint` clean; `vitest` 11/11**, no regressions. Smoke-tested through the dev server: `/moderation` still 200s, `GET /api/admin/users` (with and without query params) 401s unauthenticated, matching every other `/api/admin/*` route.
- **Not committed** — bundled with the OTP work above into the same pending, uncommitted batch; awaiting the user's go-ahead.

## Ops: Resend sending address moved off the sandbox domain (2026-08-26)

Explicit ask: change the Resend `EMAIL_FROM` to `noreply@xoldout.app`. Confirmed with the user first that `xoldout.app` is already verified as a sending domain in their Resend account — switching off the sandbox domain (`onboarding@resend.dev`) without that would have made every transactional email (order confirmations, moderator OTP codes, etc.) silently fail, since `sendEmail()` logs and swallows a failed Resend call rather than throwing.

- **`EMAIL_FROM` is entirely env-driven** (`process.env.EMAIL_FROM` in `lib/email.ts`) — no code change was needed, just the value. Updated in three places: `web/.env.local` (local dev), `web/.env.local.example` (onboarding default + comment updated to explain the fallback), and the Vercel **Production** environment variable (removed and re-added via `vercel env rm`/`vercel env add`, since the CLI has no in-place update).
- **No redeploy triggered or needed** — `EMAIL_FROM` is a server-only (non-`NEXT_PUBLIC_`) variable, read at request time inside Vercel Functions, not inlined into the client bundle at build time. It takes effect on the next invocation of any route that calls `sendEmail()`.

## Email-sent toast confirmation; super-moderator promotion; compact user list (2026-08-26)

Three follow-up asks in quick succession.

**Toast when a user-triggered email actually sends.** Scoped, via a clarifying question, to every flow where a user action directly causes an email and the client is waiting on the result: the moderator OTP code (send + resend) and the account-deletion recovery email. Deliberately *not* order confirmations — those are fire-and-forget (`void sendOrderConfirmationEmail(...)`) from a webhook/order route the client already got a response from before the email attempt even starts, so there's no truthful "sent" moment to toast from without restructuring that path to block on it, which wasn't asked for.

- **`sendEmail()` now returns `Promise<boolean>`** (true only on a confirmed 2xx from Resend) instead of `Promise<void>` — previously a failed or skipped send was invisible to every caller, logged and swallowed. `sendModeratorOtpEmail`/`sendAccountDeletedEmail` return that boolean through; `sendOrderConfirmationEmail`/`sendDigestEmail` (still fire-and-forget/cron, no client waiting) ignore it, unchanged.
- **`POST /api/admin/otp/request` and `DELETE /api/me`** both now include `emailSent: boolean` in their JSON response. `ModeratorOtpForm` and `DeleteAccountSheet` toast success on `true` and toast a distinct warning on `false` ("couldn't confirm the code/recovery email went out — check spam, or resend") rather than assuming delivery — the request itself can still succeed (code issued, account soft-deleted) even if Resend silently failed, and that gap was invisible before this pass.

**Super-moderators can now promote/demote other moderators.** Explicit reversal of the prior pass's deliberate design ("isSuperModerator stays DB-only, same as isModerator's bootstrap") — the user asked directly for this capability. `POST /api/admin/moderators` takes an optional `isSuperModerator` alongside the existing `isModerator`, refuses to demote the *last* remaining super-moderator (would leave nobody able to manage moderators, including re-promoting themselves — enforced with an `id: { not }` count check, not a self-demotion-specific check, so it also covers one super-moderator demoting a different one when they're the only other), and clears `isSuperModerator` automatically when `isModerator` is revoked (a non-moderator can't stay a super-moderator). `ManageModeratorsPanel` gained "Make super"/"Remove super" buttons per row, with a `window.confirm` on promotion only (demotion/revoke already had no confirm step in this panel, unchanged). Verified live: 5 assertions against the local dev DB (promote, demote-with-another-super-present, refuse-demoting-the-last-one, status unchanged after a refused attempt, revoking isModerator clears isSuperModerator too) — all passed.

**Users list made compact + max 5 rows.** Follow-up on the same-day `UsersListPanel`: `PAGE_SIZE` in `GET /api/admin/users` dropped from 25 to 5, and each row collapsed to a single line (name/handle/badges only, same expand-affordance chevron as `VerificationQueuePanel`) that expands on click to reveal email/joined-date/listing-count/"View" link — previously every row rendered both lines unconditionally. `expandedId` resets to `null` whenever the search query, `includeDeleted`, or `page` changes, so a stale expanded row from a previous result set can't linger.

**OTP is no longer sent automatically on mount.** The original design auto-requested (and emailed) a fresh code the instant `ModeratorOtpForm` mounted — which fires every single time the 60s inactivity window (`useModeratorSession`) lapses and bounces someone back to this branch, even for a moderator who just glanced away and came straight back. Explicit ask: only send when the moderator actively asks. Removed the mount `useEffect` entirely; the form now opens on a "Send code" prompt (`sent === false`) and only calls `POST /api/admin/otp/request` when that button (or the post-send "Resend code") is clicked. This also removes the `react-hooks/set-state-in-effect`-driven duplicated-function pattern used everywhere else this session, since there's no longer any effect-body async work to route around.

**Verified**: `tsc --noEmit`/`eslint` clean across every touched file (`lib/email.ts`, `app/api/admin/otp/request/route.ts`, `app/api/me/route.ts`, `app/api/admin/moderators/route.ts`, `app/api/admin/users/route.ts`, `app/(app)/moderation/page.tsx`, `components/profile/DeleteAccountSheet.tsx`); `vitest` 11/11. Smoke-tested through the dev server: `/moderation` 200s, and `GET /api/admin/users`, `POST /api/admin/moderators`, `POST /api/admin/otp/request`, `DELETE /api/me` all 401 unauthenticated. Not committed — bundled with the earlier same-day OTP/growth-chart/users-directory work, still awaiting the user's go-ahead.

## Password reset link on /login (2026-08-26)

Explicit ask: users should be able to recover a forgotten password from the login page. Used Firebase Auth's built-in `sendPasswordResetEmail` client SDK call rather than a custom flow — Firebase sends and hosts the reset email/link itself (its own project-configured templates), so no new backend route, email template in `lib/email.ts`, or token storage was needed.

- **`LoginForm` gained a third render mode** (`"login" | "reset"`, alongside the existing `deletedHandle` branch) — a "Forgot password?" link under the password field switches to a small reset form (email input, "Send reset link"), pre-filled with whatever was already typed in the login email field. "Back to log in" returns to the normal form.
- **Never confirms or denies whether an email is registered.** `sendPasswordResetEmail` throws `auth/user-not-found` for an unregistered email on this Firebase project's current settings (Email Enumeration Protection is off) — that specific error is caught and treated exactly like success (the same "if an account exists, we've sent a link" message), so this form can't be used to check which emails have accounts. Every other error (bad email format, network failure, rate limit) still surfaces via Firebase's own message, matching how every other auth form in this app already handles Firebase errors.
- **Moderators don't need a separate reset path** — confirmed earlier this session that `/moderation`'s login uses the exact same Firebase Auth account as the regular `/login`, so this one reset flow covers both.
- **Verified**: `tsc --noEmit`/`eslint` clean, `vitest` 11/11. Live-clicked through the dev server: the link renders, switches to the reset view, submitting shows a loading state, and — this part not fully live-completable — a real production password-reset send couldn't be observed end-to-end in this environment, because the automated browser tool's network access to Google's `identitytoolkit.googleapis.com` is blocked (503 on every call, the same category of restriction that blocked verifying the moderation dashboard earlier this session). What *was* confirmed live: submitting produces the correct loading state, and a genuine Firebase error (`auth/network-request-failed`, from the blocked call) surfaces exactly as designed rather than being silently swallowed — proving the non-`user-not-found` error path works. The `auth/user-not-found` → generic-success masking itself is code-reviewed logic, not click-tested, since no live call succeeded or failed with that specific code in this environment.

## Branded password-reset email via Resend, replacing Firebase's plain-text default (2026-08-26)

Follow-up to the pass above: the user actually received a live reset email and found it was Firebase's stock template — raw pasted URL, no button, can't be restyled (Firebase's built-in template editor is rich-text only, no custom HTML/CSS). Explicit ask, after being offered "just relabel the link text via Firebase Console" vs. "real styled button via Resend": the full Resend option.

- **New `POST /api/auth/reset-password`** (deliberately unauthenticated, same reasoning as `POST /api/email/unsubscribe`): validates `{ email }` with zod, calls `adminAuth().generatePasswordResetLink(email)` (firebase-admin, server-side) to get the actual reset link, then sends it through a new `sendPasswordResetEmail()` in `lib/email.ts` — same raw-HTML-string-with-a-real-`<a>`-button pattern as `sendAccountDeletedEmail`, not Firebase's own send.
- **Still never confirms or denies whether an email is registered**, same standing rule as the client-only version it replaces. `generatePasswordResetLink` throws `auth/user-not-found` for an unregistered email; that's caught and swallowed (logged only for genuinely unexpected error codes), and the route *always* responds `200 { ok: true, emailSent }` regardless. `emailSent` is only ever `true` when a real account existed and Resend confirmed the send — but the client (`LoginForm`) deliberately does not branch UI on it, unlike the OTP/delete-account flows that toast a distinct warning on `emailSent: false`. Those are authenticated flows where enumeration isn't a concern; this one is signed-out, so it keeps the single unconditional "check your email" message.
- **`LoginForm.handleResetPassword`** now calls this route via `apiFetch` instead of the client `sendPasswordResetEmail` SDK call; the `auth/user-not-found`-specific catch it used to need is gone since the server already does that masking.
- **Verified live against the real local dev DB and the real `xoldoutpro` Firebase project**: curled the route directly — a registered email (`chibuzodaniel71@gmail.com`) returned `emailSent: true` and a real branded email arrived; an unregistered address returned `emailSent: false` with the same `200` shape (no enumeration signal in status code either); a malformed address returned `400` with the zod validation error. Repeated the registered-email call 5 more times back-to-back, all `emailSent: true` — confirms it wasn't a fluke. One cold-start call hit a transient `auth/internal-error` ("INTERNAL ASSERT FAILED: Unable to create the email action link") from Firebase Admin on the very first hit to the newly-compiled route; every call before and after it succeeded, so treated as a one-off dev-server compile artifact, not a real bug. Also clicked all the way through the actual `/login` → "Forgot password?" → submit flow in the browser and confirmed the `POST` succeeded via server logs (the browser tool's screenshot capture was separately flaky this session, unrelated to the app). `tsc --noEmit`/`eslint` clean on all touched files; `vitest` 11/11.
- **Not committed** — awaiting the user's go-ahead, same standing rule as every other feature.

## Local dev DB connection resets on /discover — environment issue, not a code bug (2026-08-26)

While testing the pending fanbase/group-chat changes, `/discover` was throwing `PrismaClientKnownRequestError P1017 ConnectionClosed` ("Server has closed the connection") on every request. Root-caused with a raw `pg` `Client`/`Pool` test script outside of Next entirely: the embedded local `prisma dev` Postgres server itself (the `xoldout` named instance, not production Neon) had wedged into a state where it `ECONNRESET`s every new connection, including `prisma migrate status` and plain `pg` connections — not specific to `lib/db.ts`'s adapter or any app code. Matches the existing warning comment in `lib/db.ts` about this local server dying outright rather than queueing once its connection cap is exceeded.

- **Fix**: `npx prisma dev stop xoldout && npx prisma dev start xoldout`. Confirmed stable with 5 consecutive raw `pg` connections, then `/discover` clean over multiple reloads and in the browser.
- **No code change** — nothing to commit here. Left as a note for future sessions: if the local dev DB wedges like this again, cycling the `prisma dev` server is the fix, not debugging `lib/db.ts`.
- **Update, same session**: the wedging recurred repeatedly and with increasing frequency over the following hour — within seconds of any real load (a `vitest run`, a couple of `curl` loops), not just once. Multiple `stop`/`start` cycles kept temporarily fixing it, but each fix window got shorter. This now looks like a genuine local-environment reliability problem with this particular embedded `prisma dev` instance, not a one-off fluke — worth recreating the local dev DB (`npx prisma dev rm xoldout` + a fresh `migrate dev`) or moving to a plain Docker/native Postgres for local dev if it keeps happening, rather than continuing to cycle it per-session.

## Vercel Image Optimization quota — laddered images were being re-transformed unnecessarily (2026-08-26)

Explicit ask, after the user got a "75% of free tier Image Optimization Transformations" alert from Vercel: find and fix what's driving the usage.

- **Root cause**: `lib/images.ts`'s `artworkLadder()` already generates correctly-sized JPEG derivatives (64/256/1024px, quality 82) at upload time for every release/beat/merch/event cover — the card size (256) and detail-hero size (1024) are already exactly right. But every `<Image>` rendering one of these ladder URLs was still going through Vercel's Image Optimization pipeline (no `unoptimized`), and for any `sizes="33vw"`/`sizes="100vw"` image (every card and every detail-page hero), Next generates a full `deviceSizes` srcset — up to 8 width variants (640–3840px) — each a separately billed transformation, most of them *upscaling* past the 256px/1024px source for zero visual benefit. Every unique product/event/beat/merch image was paying this tax on first view, which at catalog scale is the obvious way to burn through a 5,000-transformation free tier fast.
- **Fix**: added `unoptimized` to the 8 `<Image>` call sites whose `src` is a laddered R2 URL — `ProductCard`, `EventCard`, `EntitlementCard`, `MiniPlayer` (`current.artworkUrl`, also laddered), and the four detail-page heroes (`/r`, `/b`, `/m` — primary image only, not the gallery — and `/e`). Every `<Image>` in the app already sets an explicit `sizes` prop (nothing was found relying on the `fill`-without-`sizes` full-deviceSizes-sweep footgun), so this ladder mismatch was the actual driver, not a missing `sizes`.
- **Deliberately left untouched** (still needs the optimizer's real resizing, since these aren't laddered): `PostCard`'s `post.imageUrl` (`lib/uploadImage.ts` uploads the raw file with no client-side resize for posts, unlike avatars/covers) and `MerchDetailPage`'s `galleryImageUrls` (same — raw uploads, no ladder). Also left alone: avatars, group covers, profile banners — these go through `ImageCropModal` client-side to a single fixed 512px output (confirmed via `CreateFanbaseSheet`'s `resizeSquare(original, 512)` pattern), not a ladder, and are requested at small fixed-pixel `sizes` (24–72px) — Next only pulls 1-2 variants from the small `imageSizes` array for those (not the 8-wide `deviceSizes` sweep), so the quota impact was always low there and the optimizer's downsizing is genuinely useful.
- **Verified**: `tsc --noEmit`/`eslint` clean on all 8 touched files; `vitest` 11/11 (passed twice, in the windows the local dev DB was healthy — see the DB-wedging note above). Could not do live pixel-level verification because the local dev DB has no seeded product/event data to render through these components — the change itself is data-independent (a rendering-path opt-out of a well-documented Next.js `<Image>` prop), so this was judged low-risk enough to ship without that.
- **Committed in two parts**: the four detail-page heroes (`/r`, `/b`, `/m`, `/e`) landed in the earlier Fanbase/badges/toast commit since `PublishedByYou` touched the same lines in those files; `ProductCard`, `EventCard`, `EntitlementCard`, and `MiniPlayer` are this commit.

## Local dev DB — recreating it did NOT fix the wedging (2026-08-26)

Explicit ask, following the escalating `prisma dev` instability noted above: recreate the local dev DB from scratch.

- **Did**: `npx prisma dev rm xoldout --force`, then `npx prisma dev --name xoldout --db-port 51214 --shadow-db-port 51215 --port 51213 -d` (pinned to the ports `.env` already expects, so no `.env` change needed). The freshly-created instance came up with the full app schema already present (all tables, no `_prisma_migrations` row) — apparently `prisma dev` clones new instances from some local template/cache rather than a truly blank Postgres, which made `prisma migrate dev` fail with P3005 ("schema not empty"). Confirmed with the user before running it, then `DROP SCHEMA public CASCADE; CREATE SCHEMA public;` against the local instance (port 51214 only, no data existed to lose), followed by a clean `prisma migrate deploy` — all 7 migrations applied, `migrate status` reported up to date.
- **The wedging is not fixed.** `vitest run` passed once immediately after the rebuild, then failed with the identical `PrismaClientKnownRequestError P1017 "Server has closed the connection"` on the very next run seconds later — same signature as every prior occurrence, now reproduced on a database that had never held a single row. This rules out corrupted data/schema state as the cause. The instability is with the embedded `prisma dev` engine's process itself (this specific machine, this specific `prisma@dev v0.16.28` build) under repeated connect/disconnect cycles, not anything about the `xoldout` instance's history.
- **Current workaround, unchanged**: `npx prisma dev stop xoldout && npx prisma dev start xoldout` (as a plain foreground/blocking call — the `-d`/detached form has intermittently failed to actually persist the server this session) buys a working window of anywhere from a few requests to a few minutes before it wedges again. No code fix is possible for this from within the app.
- **Recommendation for a durable fix, not attempted this session** (bigger change, needs the user's call): drop `prisma dev`'s embedded engine for local dev entirely in favor of a real local Postgres — either a native Windows install or Docker (Docker wasn't available in this environment: `docker: command not found`) — and point `DATABASE_URL` at that instead. `prisma dev` is a convenience wrapper; it is not designed to be this fragile, so this may be specific to something about this machine (resource limits, an AV/security product interfering with the child process, or a bug in this version) rather than expected behavior worth working around indefinitely.

## Fanbase chat opens at the first unread message, not always the bottom (2026-08-26)

Explicit ask: opening a group with no unread messages should land at the bottom (the latest message, same as any chat app); opening one *with* unread messages should instead start right at the first unread one, so scrolling down from there reads through the rest of what's new — not always dumping the reader at the very bottom past everything they haven't seen, and not always at the top either (the previous behavior — there was no scroll management at all before this, so a long thread actually opened wherever the browser's default scroll position happened to land, typically the top).

- **`GET /api/groups/[id]/posts`** now captures `membership.lastReadAt ?? membership.joinedAt` into `readWatermark` *before* the existing fire-and-forget update bumps `lastReadAt` to `now()`, and returns it in the response alongside `posts` — the same "unread since" value the Fanbase list's badge count (`GET /api/groups`) is already computed from, just also handed to the client that's about to render the thread.
- **`GroupDetailClient`** stores that `readWatermark`, then a `useLayoutEffect` (fires before paint, so there's no visible flash at the wrong position) that runs exactly once per thread — guarded by a ref, not a dependency, so it doesn't re-fire when `messages` updates later from sending a reply — finds the first message with `createdAt` after the watermark and calls `scrollIntoView({ block: "start" })` on it (each message row now carries a ref, keyed by id, in a `Map`); if none is found (nothing unread, or the watermark is unknown), it falls back to `container.scrollTop = container.scrollHeight` — the bottom.
- **Verified end-to-end against the real local dev DB and real Firebase Auth**, not just read: minted real Firebase ID tokens for two scratch members (via `adminAuth().createCustomToken` + a `signInWithCustomToken` REST exchange — needed an explicit `Referer` header since the web API key is HTTP-referrer-restricted) against a scratch group with 5 messages straddling a watermark. Reader A (watermark placed between message 2 and 3) got back `readWatermark` matching exactly what was set, posts in correct chronological order, and computed first-unread as message 3 — correct. Reader B (watermark after all 5 messages) computed no first-unread at all, confirming the bottom-scroll fallback path is what would fire. All scratch users/rows deleted after. Could not additionally click through the actual scrolled browser view — Firebase's `identitytoolkit.googleapis.com` sign-in flow is blocked specifically inside the Chrome browser-automation tool this session (server-side `fetch`/`curl` from the terminal reach it fine, which is what made the above possible at all) — but the client's scroll logic is a direct, low-risk consumer of this now-verified data shape (a `.find()` over the same comparison already proven correct, then one DOM API call). No React component-test infra exists in this project (no `@testing-library/react`/jsdom) to add a lighter-weight check instead.
- **`tsc --noEmit`/`eslint` clean**; `vitest` 11/11 (unaffected — no existing test covers this surface).
- **Not committed** — awaiting the user's go-ahead, same standing rule as every other feature.

## Image Optimization config hardening — Vercel's own suggestions (2026-08-26)

Follow-up to the earlier `unoptimized`-on-laddered-images fix: the user pasted Vercel's own follow-up email listing six standard levers for cutting Image Optimization usage. Implemented the three that were clearly safe and beneficial for what's left (post images, merch gallery, avatars/covers/banners — the genuinely unladdered images still going through the optimizer); skipped the rest with reasons below.

- **`images.formats: ["image/webp"]`** (was the Next default `["image/avif", "image/webp"]`) — avif costs roughly double the transformations for a format-size win not worth it at this app's quality/size, per Vercel's own suggestion. This alone should meaningfully cut the *remaining* optimizer traffic, independent of the laddered-image fix.
- **`images.qualities: [75]`** — confirmed via grep that no `<Image quality=...>` call site exists anywhere in the app (every one already uses the default 75), so this is a pure lock-in: zero behavior change today, forecloses a future accidental multiplier.
- **`images.minimumCacheTTL: 2678400`** (31 days, was the Next default 60s) — safe because every image URL in this app is immutable per-upload (`lib/uploadImage.ts` generates a fresh random R2 key on every upload; nothing ever changes at an existing URL), so there's no staleness risk from caching a transformed variant for a month.
- **Deliberately skipped**: trimming `images.deviceSizes`/`imageSizes` — the suggestion needs real audience/device-mix data to do safely (over-trim and desktop viewers get visibly softer images), and the dominant cost driver (the laddered product/event catalog images) is already fully off the optimizer via `unoptimized`, so the remaining exposure through the full `deviceSizes` sweep is just single post-detail images, not a whole catalog grid — much lower marginal payoff for the quality risk. Also skipped: `remotePatterns`/`localPatterns` tightening (already properly scoped to just the R2 bucket + Google avatar hostname, nothing to remove) and "unoptimized for small/vector/animated images" (no SVG/GIF ever enters this app — `lib/uploadImage.ts` only accepts jpeg/png/webp).
- **Verified**: `tsc --noEmit`/`eslint` clean on `next.config.ts`. Since Next doesn't hot-reload `next.config.ts`, restarted the dev server fully and confirmed a clean boot (`✓ Running next.config.ts took 31ms`, no config validation errors) and that `/discover` and `/login` still render (200). `vitest` untouched by this change (its one failure this run was the same pre-existing local-DB flakiness documented above, unrelated to a config-only edit).
- **Not committed** — awaiting the user's go-ahead, same standing rule as every other feature.

## Double-tap-to-like on the social feed, with an Instagram/X-style flying heart (2026-08-26)

Explicit ask: double-tap a post's photo (or the post itself, for a text-only post) to like it, double-tap again to unlike, and show a heart emoji animating from the tap point down into the like button on a like.

- **`PostCard`**: manual timestamp-based double-tap detection (`lastTapRef`, a 300ms window) rather than the native `onDoubleClick`, so touch taps and desktop clicks behave identically and the handler gets the tap's `clientX`/`clientY` for the animation's start point. Wired onto the image container when the post has a photo, or the body `<p>` when it doesn't (kept off the body when there *is* a photo, so double-tapping a link inside `Linkified` caption text can't be mistaken for a like gesture). Both call the same, unmodified `toggleLike()` the existing like button already used — this is a second trigger for the same action, not new like/unlike logic.
- **`FlyingHeart`** (new, local to `PostCard.tsx`): only spawned on the like transition (not on unlike, matching the existing button's behavior of never showing one either). `position: fixed` at the tap coordinates, then one `requestAnimationFrame` later its `transform`/`opacity` change to the like button's live `getBoundingClientRect()` center — a CSS transition animates between the two states, giving the "pop in, fly down into the button, shrink, fade" motion. `fixed` positioning was chosen specifically so the heart isn't clipped by the photo container's own `overflow-hidden` on its way out.
- **Verified**: `tsc --noEmit`/`eslint` clean. Full authenticated browser click-testing wasn't possible — `identitytoolkit.googleapis.com` is blocked from *any* code path inside the Chrome browser-automation tool this session (not just form submission; confirmed this applies even to a direct SDK call), so there's no way to sign in through that tool. Instead, since this is fundamentally a visual/animation feature, built a standalone (no-auth, no-app) HTML reproduction of the exact same double-tap-detection and fly-to-button transform logic, served locally and double-clicked through the real browser: confirmed the toggle fires correctly on a real double-click (count and fill state updated), and — by temporarily slowing the transition to 3s for visual QA only, in the throwaway test file, not the app — caught the heart genuinely mid-flight in a screenshot, partway between the tap point and the button, fading and shrinking correctly, confirming the animation moves *toward* the button rather than just fading in place. The throwaway test harness was deleted after.
- **Not committed** — awaiting the user's go-ahead, same standing rule as every other feature.

## Full email design system — 8 templates, 3 of them new sends (2026-08-27)

Explicit ask: the user supplied a reference HTML file (8 dark/red-accented XOLDOUT email templates — Welcome, Payment successful, Payment failed, Sale notification, Reset password, OTP, Weekly digest, General/catch-all) and asked to "work on all these templates." 5 of the 8 mapped to emails the app already sends (`sendOrderConfirmationEmail`, `sendDigestEmail`, `sendModeratorOtpEmail`, `sendPasswordResetEmail`, `sendAccountDeletedEmail`); 3 (Welcome, Payment failed, Sale notification) didn't correspond to anything the app sends today. Asked the user to scope it via `AskUserQuestion` — restyle only, or also build the 3 new sends — and they chose the full 8.

**Shared design system, `lib/email.ts`**: extracted the reference's repeated pieces into small builders — `shell()` (the dark canvas + bordered card + shared header/footer every template sits in), `header()`/`footer()`, `button()`, `iconCircle()` (the ringed glyph used by Welcome/Payment-success/Payment-failed/Reset), and `receiptCard()` (the dark boxed key/value rows with a colored left accent bar, used by Payment success/failed/Sale notification). Every dynamic string still goes through the existing `escapeHtml()` — that protection didn't get any looser in the rewrite.

- **New `sendWelcomeEmail`** (template 01) — fired from `POST /api/auth/sync` right after the mirrored Postgres `User` row is created on true first sign-in (the one place both email/password and Google sign-up converge), fire-and-forget so a stalled send can't block the client's redirect to onboarding.
- **`sendOrderConfirmationEmail`** (02) restyled onto the receipt-card layout; gained an optional `processor` field (Payment.processor — "Flutterwave"/"Monnify") shown as a row in place of the reference's card-brand "Method" row, since this app has never captured card-brand/last-4 data anywhere and fabricating it wasn't the ask. The ticket/QR block (for event purchases) was restyled to the dark palette but kept as-is structurally.
- **New `sendPaymentFailedEmail`** (03) — fired from `finalizePayment`'s (`lib/commerce/confirmPayment.ts`, shared by both the Flutterwave and Monnify webhooks) failure branch, which previously sent the buyer nothing at all on a declined/failed charge. Needed a product fetch that branch didn't previously make (for the title and, via a new local `productHref()` — the same per-type `/b`/`/m`/`/r`/`/e` routing `ProductCard.tsx`'s `hrefFor` already uses client-side, reimplemented server-side since that one's a client component — the "Try again" retry link).
- **New `sendSaleNotificationEmail`** (04) — fired from `finalizePayment`'s success branch, alongside the pre-existing in-app `SALE` notification (creators previously only got an in-app badge, never an email, on a sale). Shows the *net* amount (gross minus the 15% platform commission, `lib/commerce/ledger.ts`'s `COMMISSION_RATE` — the same rate `recordSale` already deducts) rather than the gross sale price, since "added to your wallet" is a specific, checkable claim and the gross figure would overstate it. Wallet balance shown is `availableKobo + pendingKobo` from the existing `getWalletBalances()`.
- **`sendPasswordResetEmail`** (05) and **`sendAccountDeletedEmail`** (08) restyled onto the icon-circle and general/eyebrow-headline-button shapes respectively — no behavior change, same recipients/triggers as before.
- **`sendModeratorOtpEmail`** (06) restyled onto the reference's six individually-boxed digits (`input.code.split("")`) in place of the old single big number.
- **`sendDigestEmail`** (07) kept its existing two-section data shape (a highlighted top song, then a numbered "recommended" list) rather than forcing it into the reference's single ranked-chart layout — the digest's actual data (one top song plus unrelated recommended products) doesn't carry play-count rankings for a real unified chart, so the new numbered-row visual treatment was applied to the list it does have instead of inventing data to fit the reference exactly.
- **Verified two ways.** (1) Actually sent all 9 variants (all 8 templates, plus the buyer-confirmation email a second time with a real generated ticket QR code) through the real Resend API to the user's own inbox for visual inspection — every send confirmed 2xx. (2) For the two new, more complex integration points, called `finalizePayment` directly against real seeded local-dev-DB data (a scratch creator/buyer/product/order/payment, not mocked) for both outcomes: the success path produced the exact expected ledger entries (`SALE_CREDIT +100000`, `COMMISSION_FEE -15000` — precisely 15% of a ₦1,000 sale) and flipped the order to `PAID`; the failure path flipped it to `FAILED` and resolved the retry link to `/r/<id>` correctly for a RELEASE-type product. Both email calls fired as fire-and-forget promises exactly as coded — confirmed by Resend correctly *rejecting* them, since the scratch users had fake `@example.com` addresses, which is proof the call happened, not a code bug. `tsc --noEmit`/`eslint` clean across every touched file (`lib/email.ts`, `lib/commerce/confirmPayment.ts`, `app/api/auth/sync/route.ts`); `vitest` 11/11, dev server confirmed booting clean and serving `/`, `/login`, `/discover`.
- **Debugging note for future sessions**: while building the `finalizePayment` integration test, hit a red herring — a `PrismaClientKnownRequestError P1010 DatabaseAccessDenied` that looked like new, worse local-DB corruption but was actually a bug in the *test script*: a static `import { db } from "./lib/db"` gets hoisted ahead of a `dotenv.config()` call textually above it, so `lib/db.ts`'s module-level `process.env.DATABASE_URL` read ran before the env was populated. Fixed by dynamically `await import()`-ing `./lib/db` after calling `config()`. Worth remembering before concluding the local DB itself has degraded further — check the script's import order first.
- **Not committed** — awaiting the user's go-ahead, same standing rule as every other feature.

**Follow-up, same day**: three explicit tweaks after the user saw the sends. (1) Welcome email body copy replaced — was "Upload beats, drop merch, or sell out a show — the marketplace is yours to run," now "Upload your music, sell beats and merch, promote your shows, and connect with your fans — all from one place." (2) The shared `header()` (every one of the 8 templates renders through `shell()`, so this changed all of them, not just Welcome) swapped its CSS-styled "XO / XOLDOUT" text wordmark for the real app icon image (`public/xoldout-icon-transparent.png`, referenced by its production URL since email `<img>` needs a real reachable address) next to the "XOLDOUT" text — the same file `AppHeader.tsx`, the favicon, and the mini-player watermark already use, so every place the brand shows up is the same image rather than a fourth CSS approximation of it. (3) Removed the "You're in the crate." headline entirely per explicit ask; "Your XOLDOUT account is live." moved up from the body into the now-empty headline slot (keeping the same headline+supporting-text shape rather than leaving a bare paragraph under the icon), and the subject line changed to match. Verified each round: `tsc --noEmit`/`eslint` clean, confirmed the image resolves at `https://www.xoldout.app/xoldout-icon-transparent.png` (200, `image/png`), and re-sent the welcome email after every change for a live visual check.

## Feed and Fanbase list now prioritize recency over ranking (2026-08-27)

Explicit ask, after a clarifying question (which "feed" — Socials or the Fanbase groups list): both, prioritize recency across all of them.

- **`GET /api/posts`**: dropped the `forYou` mode's affinity-based reordering (`scoreForYou`/`fetchForYouSignals`, `lib/socials/forYouRanking.ts`) — both `following` and `forYou` now return their candidate pool straight from `orderBy: { createdAt: "desc" }`, unreordered. What still differs between the two tabs is only the *pool* (followed creators + self, vs. everyone except self) — same distinction the route already documented, just without a ranking step changing order within it anymore. Also dropped the now-pointless wider 150-candidate pre-fetch for `forYou` (it existed solely to give the ranking step something to reorder among) back to the same 50 both modes actually return. `lib/socials/forYouRanking.ts` and its test (`forYouRanking.test.ts`) were left in place, just unused by this route — a documented, tested subsystem, not deleted on the assumption it won't matter again.
- **`GET /api/groups`**: was ordered by `FanbaseGroup.createdAt` (when the group was *made*), which doesn't track activity at all — a group created last month with a message five minutes ago would rank behind a group created yesterday with no messages since. Now re-sorted, after the last-message lookup the WhatsApp-style row preview already needed, by last-message time (falling back to the group's own `createdAt` for a message-less group, so it still gets a defined position instead of sorting undefined-last). The initial DB query still pre-filters to the newest 50 *groups* by creation date before this re-sort — a cheap proxy that's exact at this app's join-a-handful-of-groups scale, not a full join-and-sort in SQL.
- **Verified**: `tsc --noEmit`/`eslint` clean on both routes. `vitest`: `forYouRanking.test.ts` (unaffected — pure function, not touched) passed; `stock.test.ts`'s failures were the same pre-existing local-DB flakiness documented earlier in this file, unrelated to either changed route. Hit a separate, genuinely new local-only issue while smoke-testing live — a Turbopack panic on Windows (`node process exited ... 0xc0000142`) compiling `globals.css`, taking `/socials` and `/discover` down with 500s — fixed by deleting the stale `.next` build cache and restarting; confirmed stable over 3 repeated loads afterward. Unrelated to this change (nothing here touches CSS or build config) and not expected to recur on its own.
- **Not committed** — awaiting the user's go-ahead, same standing rule as every other feature.

## Bachs added as a third checkout processor, and the only one buyers currently see (2026-08-31)

Explicit ask: "use app.bachs.io as the payment gateway since others have not been approved yet." Flutterwave and Monnify's merchant accounts are still pending approval, so checkout through either was effectively unusable for real buyers; Bachs (bachs.io — a REST-based checkout/billing platform, docs at docs.bachs.io) is wired in as a third processor and made the only one the buyer-facing picker offers, without removing the other two from the codebase.

- **`lib/bachs.ts`** (new), same shape as `lib/flutterwave.ts`/`lib/monnify.ts` deliberately — `initializePayment`, `verifyTransaction`, `initiateRefund`, all in `amountKobo` at the call site. Bachs's own API speaks decimal-string major-unit amounts (`"75000.00"`, not kobo), converted at this file's edges so nothing else in the app needs to know that. `initializePayment` calls `POST /v1/checkout-sessions` with a raw `pricing: { currency: "NGN", amount }` (Bachs's "charge a raw amount" mode — no pre-created product, since this app's prices are computed at order time) and gets back a `checkout_url` to redirect the buyer to. `verifyTransaction(chargeId)` calls `GET /v1/payments/{payment_id}` — Bachs's authoritative charge lookup — and normalizes its `succeeded`/`accepted` statuses to `"successful"` (an `accepted` charge is Bachs's term for an under/overpayment accepted as final settlement — still real money received) and everything else to `"failed"`.
- **`app/api/webhooks/bachs/route.ts`** (new). Same trust model as the other two webhooks (PRD §16): the raw body is never trusted for amounts/status, only used to know which `charge_id` to look up via `verifyTransaction`. Bachs signs deliveries differently from either existing processor — `X-Bachs-Signature` is an HMAC-SHA256 hex digest of `"{X-Bachs-Timestamp}.{raw_body}"` (not a bare shared-secret header like Flutterwave's `verif-hash`, nor an HMAC of the body alone like Monnify's `monnify-signature`) — verified here with `timingSafeEqual` plus a 300-second timestamp tolerance to reject replays, both straight from Bachs's own reference implementation. Subscribed to `collection.succeeded`, `collection.failed`, and `collection.underpaid` specifically (not the newer, broader `checkout.completed`) because all three carry `data.charge_id` directly, matching the existing "webhook hands you an id, you verify it server-to-server" shape the other two processors already use; a `charge_id` of `null` (Bachs's docs: test webhooks, legacy payments, manual reconciliation) is rejected rather than guessed at.
- **`collection.underpaid` handling**, added after the user asked why it wasn't included: an underpaid charge already resolves to a normal `FAILED` order through the existing path (`verifyTransaction`'s `status` isn't in `SUCCESS_STATUSES`, so `finalizePayment` releases stock and fails the order exactly like a decline). The one thing that path doesn't capture is that Bachs is actually holding part of the buyer's money in this case, unlike a plain decline — so `verifyTransaction`'s return type gained `amountPaidKobo` (from the same authoritative `GET /v1/payments/{id}` call, not the webhook body) and the route logs a `console.error` with the charge id, order id, and paid-vs-expected amounts whenever this event fires, so a partial payment gets manually reconciled (refund or request the balance) in the Bachs dashboard instead of silently vanishing into a `FAILED` order. No dedicated alerting channel exists in this app (checked — no Slack webhook, no admin-broadcast notification kind), so this reuses the same "log it for a future session/Vercel logs" convention this file's own debugging notes already rely on rather than building one.
- **`app/api/orders/route.ts`**: `gateway` enum gained `"bachs"`, and the default flipped from `"flutterwave"` to `"bachs"` — the fallback should be the processor that's actually live, not the one that happened to be first.
- **`app/api/reports/[id]/route.ts`** (takedown refund flow): added the `payment.processor === "bachs"` branch alongside the existing Monnify check, so a takedown on a Bachs-paid order calls `initiateBachsRefund` instead of silently falling through to the Flutterwave branch.
- **Buyer-facing picker, not a backend-only swap**: `components/checkout/GatewayPickerSheet.tsx`'s `OPTIONS` now lists only Bachs — Flutterwave and Monnify are commented as pending approval rather than deleted, so restoring them once approved is a one-line revert. `lib/useGatewayCheckout.ts`'s `Gateway` type gained `"bachs"`; the hook itself is untouched.
- **Redirect plumbing**: `/checkout/callback` (existing page, unchanged) identifies the order from a `?tx_ref=` query param that Flutterwave appends to the redirect itself. Bachs instead appends its own `?checkout_id=` and has no concept of `tx_ref`, so `lib/bachs.ts` builds `success_url` as `{redirectUrl}?tx_ref={orderId}` at checkout-session creation time — Bachs then appends `checkout_id` after that. **This assumes Bachs appends its param the way most modern checkout APIs do (`&` when a query string already exists, not a second bare `?`)** — the docs don't state this explicitly either way, and it could not be checked against a live checkout without API credentials this session. Verify this on the first real sandbox checkout: confirm `/checkout/callback` receives an intact `tx_ref` and not a value with a stray `?checkout_id=...` concatenated onto it.
- **Where the docs came from**: `bachs.io` and `docs.bachs.io` returned `403` to this session's default fetch tool (bot-detection, not an auth wall — a browser-like User-Agent via `curl` got `200`); pulled the full API reference from `docs.bachs.io/llms-full.txt` (a Mintlify docs site's plaintext export) and the exact request/response field names from `docs.bachs.io/docs/openapi/openapi.json`, rather than guessing at either.
- **Credentials since supplied**: the user provided a **live** (`sk_live_...`, not sandbox) `BACHS_SECRET_KEY`, confirmed working with a read-only `GET /v1/payment-methods` call (200) before being written to `web/.env.local` — never sandbox-tested first, so the very first real checkout through it is also the first checkout through this integration at all. `BACHS_BASE_URL` set to `https://api.bachs.io` (production) to match. The user then created the webhook endpoint at `https://www.xoldout.app/api/webhooks/bachs` (subscribed to all three events above) and supplied its `whsec_...` signing secret, now `BACHS_WEBHOOK_SECRET` in the same file. `tsc --noEmit`/`eslint` clean on every touched/new file. `vitest`: `forYouRanking.test.ts` unaffected (6/6); `stock.test.ts`'s 5 failures are the same pre-existing local-DB (`prisma dev`) flakiness documented earlier in this file, unrelated to this change.
- **Live end-to-end test, confirmed working (2026-08-31)**: deployed (see below), then the user completed two real purchases through the live site. Queried the production DB directly rather than trusting the redirect: both orders show `status: "PAID"`, `Payment.status: "SUCCESSFUL"`, with real Bachs charge ids (`providerTransactionId`, e.g. `ch_99b72a9e761e443d86c62ac06232bb1b`) — confirming checkout-session creation, real payment, webhook signature verification against an actual Bachs-signed delivery, and `finalizePayment` all worked correctly together. This also resolves the `success_url`/`?tx_ref=` uncertainty above: Bachs's redirect didn't corrupt it. First real deploy hit one gap: `.env.local` is local-only (gitignored) and never reaches Vercel — production had no `BACHS_*` env vars at all, so the first live attempt failed at `initializePayment` with `BACHS_SECRET_KEY is not set`, caught via `vercel logs`. Fixed by adding all three to the Vercel project's Production environment via `vercel env add` and redeploying (env vars only take effect on deployments created after they're added, not retroactively).

**Follow-up, same day**: explicit ask to also hide Flutterwave/Monnify from the checkout flow entirely until their API keys are in hand, rather than just from the picker's visible list — "let the default one just be the working one for now." `lib/useGatewayCheckout.ts` gained `ENABLED_GATEWAYS: Gateway[]` (currently `["bachs"]`) as the single source of truth for which processors are actually usable; `pickGateway()` now resolves straight to the one entry without ever opening the sheet when the array holds exactly one, so a buyer never sees a "choose how to pay" prompt with nothing real to choose between. `GatewayPickerSheet.tsx`'s `OPTIONS` are now derived by filtering a `LABELS` map (which still names all three gateways) down to `ENABLED_GATEWAYS`, so the two files can't drift out of sync — re-enabling Flutterwave or Monnify once approved is a one-line edit to `ENABLED_GATEWAYS`, nothing in the sheet or the four purchase components needs to change. `tsc --noEmit`/`eslint` clean.

## Custom toast system replaced with react-toastify (2026-08-31)

Explicit ask: "used React-Toastify to handle errors everywhere." The app already had a hand-rolled `ToastProvider`/`useToast()` (context + stack of animated divs, styled to the dark/red theme, used in 9 files) alongside a separate population of components that show errors as inline `setError` text instead (PurchaseAndPlayer, MerchPurchaseForm, EventTierPicker, etc.). Asked the user via `AskUserQuestion` which of three scopes this meant; they chose "fully replace ToastProvider" — swap the library underneath the existing 9 call sites, leave inline per-form error text alone.

- **`components/ui/ToastProvider.tsx`** rewritten to wrap `react-toastify@11.1.0` (the first version with a React 19 peer range — this app is on React 19.2.8) instead of a hand-rolled context/state stack. `useToast()` **kept its exact old return shape** (`{ error, success }`) specifically so none of the 9 existing call sites (`ManageGroupSheet`, `DeleteAccountSheet`, `FollowButton`, `ChatMessage`, `PostCard`, `moderation/page.tsx`, etc.) needed to change at all — they still destructure `const toast = useToast()` and call `toast.error(...)`/`toast.success(...)` exactly as before; only what those functions do underneath changed. Same icons (inline SVGs, unchanged) and same 4s auto-dismiss carried over for visual parity; `ToastContainer` uses react-toastify's `Slide` transition (not the library's default `Bounce`) to match the original's slide-only motion.
- **`app/globals.css`**: the old `.animate-toast-in`/`-out` keyframes (only ever used by the deleted implementation) were removed and replaced with `.Toastify__toast-container`-scoped overrides restyling react-toastify's default white-card look to this app's dark surface + red/green accents. **These had to be written as two-class selectors** (`.Toastify__toast-container .Toastify__toast--error`, not the bare `.Toastify__toast--error` the library's own stylesheet uses) — first attempt used the bare one-class selectors and silently lost the cascade to `ReactToastify.css`'s same-specificity rules, because Next doesn't guarantee this file's CSS loads after a library stylesheet imported from a component file. Caught by an actual browser check (see below), not by inspection — the computed background was still the library's white before this fix.
- **Verified in a real browser, not just by reading the diff**: `tsc --noEmit`/`eslint` clean. Started the dev server, built a throwaway `/toast-test` route (two buttons, unauthenticated, deleted after) since none of the 9 real call sites are reachable without signing in through Firebase Auth — blocked in this session's browser-automation tool the same way prior sessions recorded. First pass showed **no toast rendered on click at all**; console showed no output either, which turned out to be an artifact of a Fast Refresh reload landing mid-click rather than a real bug — triggering via `element.click()` in the page's own JS context (bypassing the automation tool's synthetic click) confirmed the handler and `toast.error()` call both fire correctly. That same check caught the CSS specificity bug above: computed styles read the library's white background before the fix, the theme colors (`--red-soft`/`--green`) after it. Final screenshot confirms the restyled dark/red and dark/green toasts match the original design.
- **Not committed** — awaiting the user's go-ahead, same standing rule as every other feature.

## Image loading fallback — closing a gap found by an exploratory perf/image audit (2026-08-31)

Explicit ask ("sometimes they don't get to load") plus a separate general perf audit that found the likely cause: **zero image `onError` handling existed anywhere in the app** — a failed load (a stale R2 URL, a transient network blip, a slow/rate-limited optimizer hop) rendered a broken-image icon forever with no recovery, at every avatar/cover/artwork call site.

- **`components/ui/ArtworkImage.tsx`** (new) — drop-in replacement for the `{url && <Image fill .../>}` pattern used everywhere artwork renders via `next/image` (product/event cards, the four detail-page heroes, the merch gallery thumbnail, the mini/expanded player, post images and avatars — 15 call sites across 10 files). A missing or failed `src` renders nothing, same as a missing one always did, so the parent's own `bg-surface-2` empty-state shows through either way.
- **`components/ui/FallbackImg.tsx`** (new) — same idea for the app's plain-`<img>` avatars/covers (small fixed-size UI chrome not worth restructuring onto `next/image` for — discover, search, socials, library, profile/catalog ×4, groups, gifts, chat — 13 call sites across 12 files). Takes an explicit `fallback` node so a site that already showed initials for a *missing* avatar (discover's creator strip, search's creator list) now shows those same initials for a *failed* one too, instead of only covering the "never had a URL" case it originally handled.
- **Real bug caught by an actual browser check, not just reading the diff**: a first pass using only `onError` looked right in isolation but showed broken-image icons anyway when tested against a real dead R2 URL. Root cause — on a server-rendered page the `<img src>` is already in the initial HTML, so a *fast* failure (an immediate 404, exactly what a stale URL produces) can finish loading before React hydrates and attaches `onError`; the event fires into empty air. Fixed by also checking `ref.current.complete && ref.current.naturalWidth === 0` (the standard "loaded, but broken" signature) in a `useEffect` on mount, which catches exactly the case a hydration-timed `onError` alone misses. This applies to `ArtworkImage` too — confirmed via `next/image`'s ref-forwarding to the underlying `<img>` — and matters most there, since the `priority` hero images fetch eagerly (no lazy-load deferral) and are the most likely to race hydration.
- **Verified in a real browser**: built a throwaway `/img-fallback-test` route (deleted after) rendering both components against a real dead R2 URL (`.../does-not-exist.jpg`) and a `null` src. First pass: `ArtworkImage` correctly showed nothing, but both `FallbackImg` cases still showed the browser's broken-image icon — confirmed via `img.complete`/`naturalWidth` in the page's own JS console that the image had genuinely failed to load despite the fallback not firing, which is what surfaced the hydration-race bug above. After the fix: reloaded, confirmed all four cases render correctly (initials fallback shown, `null`-fallback cases show a clean empty surface, zero `<img>` elements matching the broken alt text left in the DOM).
- **One call site the exploratory audit's grep missed**: `components/player/ExpandedPlayer.tsx`'s now-playing artwork (same plain-`<img>` pattern as `MiniPlayer.tsx`) — caught by re-grepping the whole app for `<img` after applying every flagged site, rather than trusting the original list was exhaustive.
- **`tsc --noEmit`/`eslint` (repo-wide, `--max-warnings=0`) clean; `vitest`: `forYouRanking.test.ts` unaffected (6/6); `stock.test.ts`'s 5 failures are the same pre-existing local-DB (`prisma dev`) flakiness documented earlier in this file, unrelated to this change.
- **Deliberately not done in this pass**: switching the plain-`<img>` sites to `next/image` (a secondary, separate perf recommendation from the same audit — lazy-loading/layout-shift wins, not a fallback-behavior fix) and converting client-fetching pages to server components. Scoped out per explicit ask to start with just the fallback fix.
- **Not committed** — awaiting the user's go-ahead, same standing rule as every other feature.


## Creator payouts moved from Flutterwave to Bachs (2026-08-31)

Explicit ask, after discovering a real launch-blocking gap: Vercel production's `FLUTTERWAVE_SECRET_KEY` couldn't be confirmed as a live key (Vercel makes "Sensitive" env vars write-only), and the local `.env.local` copy is a `FLWSECK_TEST-...` sandbox key — meaning creator withdrawals might have looked successful in the UI while never actually moving real money. Rather than chase down whether Flutterwave was actually approved for live transfers, moved payouts onto Bachs, the processor already confirmed live for checkout.

- **`lib/bachs.ts`** gained a Payouts section: `getNigerianBanks`, `resolveAccount`, `createPayoutDestination`, `initiatePayout` — mirroring `lib/flutterwave.ts`'s payout functions in purpose, but Bachs's model is structurally different: it requires registering a **payout destination** once (`POST /v1/payouts/destinations`) and paying out to its `id` from then on, rather than Flutterwave's flat per-call transfer with raw bank details every time.
- **`PayoutAccount` gained `payoutDestinationId`/`payoutDestinationUsable`** (migration `20260831080000_payout_destination_bachs`, hand-written SQL rather than `prisma migrate dev` — the shadow database was in a broken state, `type "GroupVisibility" already exists`, another symptom of this machine's ongoing `prisma dev` instability documented earlier; applied directly with `prisma db execute` then marked applied with `prisma migrate resolve --applied`). An existing `PayoutAccount` row from before this switch has neither set, so `withdraw` now explicitly rejects it ("This bank account needs to be re-added") instead of trying to pay out to a destination that doesn't exist.
- **Three routes swapped**: `app/api/wallet/banks/route.ts` and `payout-accounts/route.ts` now list/resolve through Bachs instead of Flutterwave (keeping bank codes consistent end-to-end — the bank list, the resolve call, and the destination registration all need to agree on the same code space, so all three had to move together, not just the money-moving call). `payout-accounts/route.ts`'s `POST` now also calls `createPayoutDestination` right after `resolveAccount`, once, at add-time — so `withdraw` never needs to register anything, only pay out to an id that already exists. `withdraw/route.ts` calls `initiatePayout` in place of `initiateTransfer`.
- **A real docs-vs-actual bug caught before it shipped**: Bachs's own docs describe `POST /v1/payouts/resolve-account` returning a wrapped `{ status, message, data, error }` envelope (matching Flutterwave/Monnify's older-style APIs). A real call against the live API, once the payout scope was granted, returned the resolved fields flat instead — no wrapper at all. Written first against the documented shape, `resolveAccount` would have thrown "Could not resolve account" on every single real call (`data.status` reading `undefined`, always falsy). Caught by actually calling the live endpoint with the platform's own already-registered bank account (`011` / First Bank, resolves to the real `XOLDOUT ENTERTAINMENT TECHNOLOGIES LTD` account name) before trusting the docs, not by reading them twice. Fixed to match the real flat shape.
- **`createPayoutDestination`'s and `initiatePayout`'s response shapes remain unverified against the live API** — verified only that the scope now permits calling them (`GET /v1/payouts`, `GET /v1/payouts/destinations`, and `GET /v1/balances` all now return `200`, previously `403 FORBIDDEN: payouts:read`), not their actual mutating behavior. Given `resolve-account`'s docs were already wrong once, treat these two as similarly unverified until a real creator adds a bank account and withdraws through the live UI — the natural next test, and lower-risk to run for real (a payout destination registration and a real withdrawal) than to fabricate against production with made-up data.
- **No key rotation needed**: the same live key (`sk_live_b94f616c_...`) was re-scoped in the Bachs dashboard rather than replacing it, confirmed via `GET /v1/payouts` flipping from `403` to `200` with an unchanged `BACHS_SECRET_KEY`. Nothing in `.env.local` or Vercel's production env needs to change for this to go live — only the code, once deployed.
- **Known pre-existing gap, unchanged by this swap**: no webhook exists anywhere in this codebase for payout completion (`payout.paid`/`payout.failed` in Bachs's terms) — `Payout.status` is set to `PROCESSING` on initiation and never automatically flips to `PAID`. This was equally true under the old Flutterwave flow (no transfer-webhook route ever existed for it either), so it isn't a regression introduced here, but is worth building before payout volume makes "did this creator actually get paid" hard to answer by hand.
- **`lib/flutterwave.ts`'s `getNigerianBanks`/`resolveAccount`/`initiateTransfer` are now dead code**, left in place rather than deleted — same precedent as keeping Flutterwave's checkout code in place behind `ENABLED_GATEWAYS` (DECISIONS.md, Bachs checkout entry above): a one-line revert if Bachs payouts ever need to fall back to Flutterwave, at the cost of one file's worth of unused exports.
- **Verified**: `tsc --noEmit`/`eslint` clean on every touched file. `vitest` 11/11 (all passing this run, including `stock.test.ts` — the pre-existing local-DB flakiness documented earlier in this file didn't reproduce today, likely because the DB was freshly restarted for the migration work above; not claiming it's permanently fixed).
- **Not committed** — awaiting the user's go-ahead, same standing rule as every other feature.
